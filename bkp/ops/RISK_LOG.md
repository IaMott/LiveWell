Risk log (append-only)

2026-03-27 20:43 | id=R-FEEDBACK-050 | severita=high | stato=mitigated | rischio=il transcript live poteva mostrare la risposta assistant prima del messaggio user a cui reagiva perche` i save client venivano inviati con fetch indipendenti non serializzati | evidenza=`src/components/chat/ChatInput.tsx` chiamava `/api/chat/transcript` per user e assistant senza coda; `src/app/api/chat/transcript/route.ts` persisteva in bulk con `createMany` senza metadata speaker | mitigazione=queue locale di persistenza live in `ChatInput.tsx` + write sequenziale `message.create` nel route transcript + test `tests/chat-input-live-ordering.test.tsx` e `tests/api/chat-transcript-route.test.ts` | rollback=`ripristinare i file dal backup locale bkp/backups/2026-03-27/2030_feedback-fix-pack`
2026-03-27 20:43 | id=R-FEEDBACK-051 | severita=high | stato=mitigated | rischio=payload/tool interni (`Payload: user.setAttribute`) potevano arrivare nel transcript, nell'export e nei preview utente | evidenza=feedback reale esportato e path codice senza sanificazione in transcript/load/export | mitigazione=`src/lib/chat/userVisibleContent.ts` applicato in scrittura transcript, load conversazione, export e preview lista; test mirati su transcript route e conversation thinking export | rollback=`ripristinare i file dal backup locale bkp/backups/2026-03-27/2030_feedback-fix-pack`
2026-03-27 20:43 | id=R-FEEDBACK-052 | severita=medium | stato=mitigated | rischio=il banner specialista poteva mostrare il lead-panel stale invece dello speaker reale dell'ultimo messaggio assistant, creando mismatch MMG/Fisioterapista | evidenza=`ChatShell.tsx` derivava il banner solo da `stateSnapshot.leadDomain`/`activeSpecialist*`; `chat/send/route.ts` preferiva la label del lead panel alla label dello speaker corrente | mitigazione=precedenza dell'ultimo `message.specialistName` nel banner e precedenza di `activeSpecialist.displayName` nel label speaker del route text; test `tests/chat-shell-specialist-banner.test.tsx` e `tests/api/chat-send-persistence.test.ts` | rollback=`ripristinare i file dal backup locale bkp/backups/2026-03-27/2030_feedback-fix-pack`
2026-03-27 20:43 | id=R-FEEDBACK-053 | severita=medium | stato=accepted-residual | rischio=la chiusura/riapertura rapidissima della sessione live potrebbe ancora intercettare un intervallo in cui la coda transcript non ha finito e il reload conversazione parte troppo presto | evidenza=`ChatShell.tsx` ricarica dopo 600ms alla chiusura live; i test coprono la serializzazione del turno ma non un close browser/SDK reale in finestra stretta | mitigazione=accettato come residuo non bloccante; se osservato in production aprire fix locale su drain/flush della coda live prima del reload | rollback=non applicabile

2026-03-26 21:17 | risk=R-F1-018 | causa=`lo storico Prisma del DB runtime era incoerente rispetto a tabelle gia esistenti (`message_reviews`, `case_states`) e impediva di applicare la nuova migrazione canonica` | sintomo=`migrate deploy falliva prima di `20260326205200_add_case_state_snapshot`` | impatto=high | detection=`errori P3018/P3009 durante prisma migrate deploy` | mitigation=`eseguiti `prisma migrate resolve --applied` sui due step storici gia materializzati e poi deploy della migrazione `state_snapshot`` | rollback=`ripristinare il backup locale 2117 e trattare il DB come bloccante esterno se la history Prisma torna incoerente` | stato=mitigated
2026-03-26 21:17 | risk=R-F1-017 | causa=`chiusura Fase 1 senza campagna finale sufficientemente formalizzata` | sintomo=`si sarebbe dichiarato DONE senza copertura esplicita di reload conversazione e live-sync canonical-first` | impatto=medium | detection=`assenza di test dedicati su route reload e response live-sync` | mitigation=`aggiunti test mirati `conversation-stateSnapshot-route` e `live-sync-stateSnapshot` e rieseguita suite minima finale` | rollback=`non dichiarare DONE e riaprire il perimetro test finale` | stato=mitigated
2026-03-26 21:17 | risk=R-F1-016 | causa=`il canonico era scritto ma il read path comune poteva ancora dipendere troppo dal legacy` | sintomo=``stateSnapshot` non sufficientemente autoritativo in presenza di conflitto con i campi legacy` | impatto=medium | detection=`inspection del read path e assenza iniziale di test conflitto snapshot-vs-legacy` | mitigation=``fromStoredCaseState()` reso canonical-first e coperto da `tests/api/case-persistence.test.ts`` | rollback=`tornare al dual-read legacy-safe se emergono regressioni severe` | stato=mitigated

2026-03-26 20:56 | risk=R-F1-017 | causa=`il read path canonico e` ora primary, ma la chiusura Fase 1 richiede ancora evidenza end-to-end text/live/reload oltre alle unit/integration tests` | sintomo=`si potrebbe dichiarare la fase chiusa solo sulla base di test parziali senza coprire transizioni chat<->live e reload` | impatto=medium | detection=`assenza di una campagna finale con smoke mirati su endpoints text/live/reload dopo il nuovo primary path` | mitigation=`eseguire subito smoke finale locale autenticato con transizioni chat->live, live->chat, reload e snapshot persistence` | rollback=`non dichiarare DONE finche la campagna finale non e` documentata` | stato=open

2026-03-26 20:53 | risk=R-F1-016 | causa=`il runtime ora persiste stateSnapshot su DB, ma il read path canonico resta ancora mediato da fromStoredCaseState che parte dalla normalizzazione legacy` | sintomo=`il canonico e` scritto davvero, ma il compat layer e` ancora troppo centrale per dichiarare chiusa la Fase 1` | impatto=medium | detection=`inspection di src/lib/ai/case/persistence.ts e assenza di test che verifichino la priorita del raw stateSnapshot rispetto ai campi legacy in conflitto` | mitigation=`downsizing immediato di fromStoredCaseState verso canonical-first puro con legacy solo fallback` | rollback=`rimuovere temporaneamente il campo stateSnapshot dal write path e tornare al dual-write preparatorio se emergono regressioni severe` | stato=open

2026-03-26 15:58 | risk=R-F1-001 | causa=`il nuovo contratto esiste solo in types.ts e non e ancora normalizzato/persistito` | sintomo=`consumer futuri potrebbero tentare di leggere stateSnapshot prima di STEP-2/STEP-3` | impatto=medium | detection=`typecheck verde ma nessun runtime consumer attivo; verificare assenza di read path sul nuovo campo prima del prossimo step` | mitigation=`mantenere i nuovi campi opzionali e non usarli nel runtime finche non esiste compat layer` | rollback=`ripristino di src/lib/ai/types.ts dal backup CP-0/CP-1` | stato=open

2026-03-26 15:58 | risk=R-F1-002 | causa=`working tree gia sporco su file memoria e fuori-scope .claude/CLAUDE` | sintomo=`rumore nel controllo stato o snapshot incompleto` | impatto=low | detection=`git status --short` | mitigation=`limitare i tocchi a src/lib/ai/types.ts e bkp/ops/*; ignorare fuori-scope` | rollback=`nessuno necessario per STEP-1` | stato=mitigated
2026-03-26 16:02 | risk=R-F1-003 | causa=`normalizeCaseState() ora sintetizza fallback legacy da payload canonical-first prima che esista compat.ts` | sintomo=`ownerAgentId/activeSpeakerAgentId ricavati in modo troppo generico o provvisorio` | impatto=medium | detection=`test futuri su mapping legacy<->nuovo e review dei payload normalizzati` | mitigation=`centralizzare il mapping definitivo in STEP-3 e considerare i fallback di state.ts come temporanei` | rollback=`ripristino di src/lib/ai/case/state.ts dal backup 1602_cp2-step2-state o disabilitazione del path canonical-first` | stato=open
2026-03-26 16:05 | risk=R-F1-004 | causa=`il nuovo snapshot canonico esiste ora come helper in compat.ts ma non e ancora serializzato in persistence` | sintomo=`i mapper funzionano nei test/typecheck ma il runtime continua a leggere/scrivere solo shape legacy` | impatto=medium | detection=`assenza di dual-write/dual-read in src/lib/ai/case/persistence.ts` | mitigation=`eseguire subito STEP-4 senza toccare ancora transport/live` | rollback=`mantenere il compat layer inattivo a livello runtime e usare solo legacy state` | stato=open
2026-03-26 16:08 | risk=R-F1-005 | causa=`toStoredCaseStateWithCanonicalSnapshot() e pronto ma non ancora usato dai caller Prisma, perche lo schema DB non espone colonne dedicate al nuovo snapshot` | sintomo=`dual-read disponibile nel modulo ma dual-write canonico non ancora effettivo nel runtime` | impatto=medium | detection=`i consumer continuano a importare solo toStoredCaseState()` | mitigation=`aggiornare i consumer nel prossimo step o introdurre una shadow-serialization controllata quando autorizzato` | rollback=`continuare a usare solo toStoredCaseState() legacy-safe` | stato=open
2026-03-26 16:19 | risk=R-F1-006 | causa=`chatPersistence.ts ora usa il nuovo dual-read ma nessun payload esterno espone ancora lo snapshot canonico` | sintomo=`il runtime server comune conosce il nuovo contratto, ma text/live client continuano a vedere solo il legacy state` | impatto=medium | detection=`assenza di stateSnapshot nei payload SSE o nelle response conversazione` | mitigation=`aggiornare prima src/app/api/chat/send/route.ts e solo dopo i consumer live/client` | rollback=`tornare a normalizeCaseState() nel read path se emergono regressioni impreviste` | stato=open
2026-03-26 16:22 | risk=R-F1-007 | causa=`il payload text ora include stateSnapshot, ma il tipo shared di chatStream e il client non sono ancora aggiornati` | sintomo=`il nuovo snapshot viaggia nel SSE ma resta inutilizzato o potenzialmente ignorato dai consumer attuali` | impatto=medium | detection=`assenza di letture di stateSnapshot in ChatContext.tsx e assenza di propagazione equivalente nel live-sync` | mitigation=`allineare prima live-sync e poi ChatContext, mantenendo i campi legacy fino alla chiusura della Fase 1` | rollback=`rimuovere stateSnapshot dal payload SSE e tornare al solo contratto legacy` | stato=open
2026-03-26 16:39 | risk=R-F1-008 | causa=`text e live-sync ora propagano entrambi stateSnapshot, ma il client non reidrata ancora il nuovo contratto` | sintomo=`lo stato canonico resta server-side e non influenza ancora banner, domini e continuita UI` | impatto=medium | detection=`ChatContext.tsx non legge stateSnapshot da SSE o loadConversation` | mitigation=`adottare il nuovo snapshot nel client prima di toccare live-token` | rollback=`mantenere il solo path legacy lato client finché non viene validato il nuovo rehydrate` | stato=open
2026-03-26 16:48 | risk=R-F1-009 | causa=`ChatContext conserva stateSnapshot ma ChatShell, LiveModal e live-token non lo usano ancora` | sintomo=`il nuovo stato condiviso esiste nel client ma non guida ancora banner, domini o bootstrap live` | impatto=medium | detection=`assenza di riferimenti a stateSnapshot nei consumer visuali e nel bootstrap live` | mitigation=`aggiornare prima ChatShell e solo dopo live-token/LiveModal` | rollback=`ignorare stateSnapshot nel context e tornare al solo legacy state se emergono regressioni client` | stato=open
2026-03-26 16:52 | risk=R-F1-010 | causa=`ChatShell usa stateSnapshot come fallback visuale da localStorage, ma il bootstrap live continua ancora a usare assunzioni singole in live-token` | sintomo=`la resa visuale della chat migliora, ma la sessione live puo ancora partire con un contesto specialistico parziale o singolo` | impatto=medium | detection=`divergenza tra banner/colore chat e system instruction live nelle sessioni successive` | mitigation=`allineare src/app/api/live-token/route.ts al nuovo snapshot prima di toccare LiveModal` | rollback=`rimuovere il fallback visuale da ChatShell e tornare al solo stato legacy se emergono incoerenze UX` | stato=open
2026-03-26 17:02 | risk=R-F1-011 | causa=`live-token ora espone stateSnapshot e bootstrap panel-aware, ma LiveModal non lo consuma ancora direttamente` | sintomo=`il backend live e piu coerente con text/chat, ma la sessione client puo non sfruttare ancora tutto il contesto shared disponibile` | impatto=medium | detection=`assenza di letture di stateSnapshot in src/components/chat/live/LiveModal.tsx` | mitigation=`adottare il nuovo snapshot nel consumer live prima di toccare ChatInput o altri adapter` | rollback=`rimuovere stateSnapshot dal payload di live-token e tornare al solo bootstrap legacy se emergono regressioni nel client live` | stato=open
2026-03-26 17:21 | risk=R-F1-012 | causa=`LiveModal ora usa stateSnapshot in modo additivo, ma i fallback legacy restano prioritari e ChatInput non e ancora allineato esplicitamente` | sintomo=`il ramo live e piu coerente end-to-end, ma in casi limite puo ancora prevalere il comportamento storico su quello canonico` | impatto=medium | detection=`differenze residue tra stato panel-aware atteso e comportamento percepito in sessioni con fallback misti` | mitigation=`mantenere l'ordine di precedenza additivo finche non emerge una necessità concreta di toccare gli adapter residui` | rollback=`rimuovere il consumo di stateSnapshot da LiveModal e tornare al bootstrap legacy puro se emergono regressioni live` | stato=open
2026-03-26 17:40 | risk=R-F1-013 | causa=`il reload conversazione ora restituisce stateSnapshot server-side, ma il client continua a usare chiavi localStorage globali non namespaced per conversationId` | sintomo=`migliora la reidratazione dopo reload, ma puo restare contaminazione cross-conversation nei fallback client` | impatto=medium | detection=`stato specialistico o snapshot che persistono passando tra conversazioni diverse` | mitigation=`chiudere il secondo blocco minimo in src/contexts/ChatContext.tsx prima di toccare altri adapter` | rollback=`ignorare stateSnapshot nel payload di reload e tornare al solo comportamento legacy se emergono regressioni immediate` | stato=open
2026-03-26 17:42 | risk=R-F1-014 | causa=`reload server-side e fallback client sono ora scoped per conversazione, ma il post-turn live non usa ancora direttamente la response di live-sync per aggiornare il client prima del reload finale` | sintomo=`la coerenza cross-conversation e chiusa, ma il ramo live puo restare un turno indietro finche non avviene il reload finale della conversazione` | impatto=medium | detection=`dopo un turno live completato, il client non riflette immediatamente il nuovo snapshot finche non si chiude la sessione o si ricarica la chat` | mitigation=`chiudere il terzo blocco minimo usando la response di live-sync nel client live` | rollback=`mantenere il comportamento attuale basato su reload finale se l'uso diretto della response live-sync introduce regressioni` | stato=open
2026-03-26 17:46 | risk=R-F1-015 | causa=`il terzo blocco minimo usa un refresh server-side della conversazione corrente per restare nel perimetro di LiveModal, ma la response di live-sync resta ancora incapsulata in ChatInput` | sintomo=`il client live si aggiorna prima del reload finale, ma l'allineamento immediato dipende da un refresh successivo e non dal payload diretto di live-sync` | impatto=low-medium | detection=`nei casi di nuova conversazione live senza conversationId iniziale il refresh immediato puo restare best-effort finche non esiste un id certo nel client` | mitigation=`se e solo se necessario in futuro, esporre in modo controllato il risultato di live-sync al consumer live senza duplicare le tool calls` | rollback=`rimuovere il refresh post-turn da LiveModal e tornare al solo reload finale se emergono regressioni nel ramo live` | stato=open
2026-03-26 22:55 | risk=R-CI-001 | causa=`il route /api/conversations/[id] ora legge prisma.caseState.findUnique ma il mock di tests/api/conversation-thinking-export.test.ts non era stato aggiornato` | sintomo=`CI rossa con TypeError su undefined.findUnique nel test di export/load conversazione` | impatto=medium | detection=`riproduzione locale dei due failure CI con vitest mirato` | mitigation=`estendere il prismaMock del test con caseState.findUnique e default null` | rollback=`ripristinare il test dal backup 2254_ci-fix-tests se il mock producesse side effect inattesi` | stato=mitigated
2026-03-26 22:55 | risk=R-CI-002 | causa=`tests/api/orchestrator-interview-flow.test.ts assumeva round2ForPersistence vuoto, ma il flow oggi persiste peer stub proposals anche senza pendingNext` | sintomo=`CI rossa con assertion failure su round2ForPersistence` | impatto=medium | detection=`confronto tra log GitHub Actions e comportamento locale di applyInterviewFlow()` | mitigation=`riallineare il test al contratto runtime attuale finche non verra cambiata esplicitamente la semantica dei peer stub proposals` | rollback=`ripristinare l'assert precedente se una futura modifica del flow rimuovera tali stub proposals` | stato=mitigated
2026-03-26 23:07 | risk=R-CI-003 | causa=`il workflow CI usava actions/checkout@v4 e actions/setup-node@v4, che nel run corrente generano warning di deprecazione per runtime Node 20` | sintomo=`pipeline verde ma warning infra persistente con futura finestra di rottura lato GitHub Actions runner` | impatto=medium | detection=`warning esplicito nel run GitHub Actions 23620073006` | mitigation=`bump minimo a checkout@v5 e setup-node@v5, che passano al runtime Node 24` | rollback=`ripristinare il workflow dal backup 2300_ci-node24-warning se emergessero incompatibilita con le action v5` | stato=mitigated
2026-03-26 23:20 | risk=R-CORE-001 | causa=`fromStoredCaseState() resta ancora facade legacy-safe per i consumer non migrati` | sintomo=`il boundary canonico esiste, ma il modulo espone ancora un bridge legacy che puo restare troppo usato se i consumer non migrano` | impatto=medium | detection=`persistenza di import da fromStoredCaseState() nei consumer server` | mitigation=`migrare per primo src/app/api/chat/send/chatPersistence.ts al nuovo readCanonicalCaseRuntimeState()` | rollback=`continuare a usare la facade attuale finche la migrazione consumer non e pronta` | stato=open
2026-03-26 23:20 | risk=R-CORE-002 | causa=`readCanonicalCaseRuntimeState() converte il fallback legacy tramite toCanonicalCaseStateSnapshot(), quindi il core e canonical-first ma non ancora legacy-free` | sintomo=`il boundary e pulito, ma il fallback storico resta concettualmente necessario per record senza snapshot` | impatto=low-medium | detection=`record storici senza stateSnapshot continuano a passare da normalizeCaseState()` | mitigation=`declassare ulteriormente il legacy nei task successivi solo dopo la migrazione del primo consumer` | rollback=`usare il backup 2315_persistence-boundary-refactor se il nuovo boundary introducesse regressioni sui record legacy` | stato=open
2026-03-26 23:35 | risk=R-CORE-003 | causa=`chatPersistence.ts espone ora path canonici espliciti, ma il route text principale legge ancora getCaseState()/persistCaseState()` | sintomo=`il primo choke point server e migrato solo a meta: il boundary canonico esiste nel deps object ma non e ancora il path primario del route` | impatto=medium | detection=`src/app/api/chat/send/route.ts continua a usare storedCaseState come input principale dell'orchestrazione` | mitigation=`migrare subito src/app/api/chat/send/route.ts al nuovo getCaseRuntimeState()/persistCaseRuntimeState()` | rollback=`tornare al backup locale 2345_cp27-chatpersistence-canonical se il route migration introducesse regressioni` | stato=open
2026-03-26 23:38 | risk=R-CORE-004 | causa=`chat/send/route.ts usa ora il path canonico come primary source, ma richiede ancora un adapter legacy locale per derive e orchestrator non ancora migrati` | sintomo=`la lettura principale e canonical-first, ma il route fa ancora una seconda lettura legacy-safe per preservare owner/protocol fields finche il core downstream non e completamente canonico` | impatto=medium | detection=`presenza simultanea di getCaseRuntimeState() e getCaseState() nello stesso route` | mitigation=`migrare i consumer server successivi e poi ridurre l'uso locale dell'adapter legacy nei route principali` | rollback=`ripristinare il backup 2350_cp28-send-route-canonical se il doppio read introducesse regressioni reali` | stato=open
2026-03-26 23:40 | risk=R-CORE-005 | causa=`live-token usa ora il boundary canonico, ma il reload conversazione e live-sync ricostruiscono ancora lo snapshot passando dalla facade legacy-safe` | sintomo=`text/send e live bootstrap sono canonical-first, ma altri consumer server chiave non hanno ancora lo stesso boundary esplicito` | impatto=medium | detection=`import/uso residuo di fromStoredCaseState() o toCanonicalCaseStateSnapshot(fromStoredCaseState(...)) nei route server` | mitigation=`migrare subito conversations/[id] e poi live-sync al read boundary canonico` | rollback=`ripristinare il backup 2342_cp29-live-token-canonical se emergono regressioni sul bootstrap live` | stato=open
2026-03-26 23:42 | risk=R-CORE-006 | causa=`reload conversazione e bootstrap live sono ora canonical-first, ma live-sync resta ancora il route piu` vicino alla semantica mono-agent e usa ancora derive legacy/locali come supporto` | sintomo=`la famiglia di route server non e ancora omogenea: l'ultimo route chiave non e ancora sullo stesso boundary e sullo stesso modello panel-aware` | impatto=medium-high | detection=`chat/live-sync continua a dipendere da storedCaseState come input operativo principale e seleziona un capabilityAgentId unico` | mitigation=`migrare subito live-sync al boundary canonico e poi affrontare in quello stesso file il cleanup della semantica tool lead-centric` | rollback=`ripristinare il backup 2344_cp30-conversations-route-canonical se il passaggio a live-sync introducesse regressioni` | stato=open
2026-03-26 23:45 | risk=R-CORE-007 | causa=`i consumer server principali sono ora canonical-first, ma i payload text/live espongono ancora campi ibridi `activeDomain`, `specialistName`, `activeSpecialistId` come compat adapter` | sintomo=`il runtime server usa il canonico internamente, ma il contratto esterno resta ambiguo e mantiene vivo il legacy nel bordo text/live` | impatto=medium | detection=`presenza dei campi legacy nei payload di chat/send e live-token/live-sync insieme a stateSnapshot` | mitigation=`procedere al cleanup del transport server snapshot-first mantenendo compatibilità transitoria esplicita` | rollback=`mantenere i campi legacy come derived fields se il cleanup rompe consumer/client non ancora aggiornati` | stato=open
2026-03-26 23:45 | risk=R-CORE-008 | causa=`i client/UI continuano a derivare parte dello stato da `activeSpecialist*` e fallback legacy locali anche se i route server sono ormai canonical-first` | sintomo=`text/live condividono il core server, ma il bordo client mantiene ancora un contratto ibrido e fallback duplicati` | impatto=medium | detection=`uso residuo di `activeSpecialistId`, `activeSpecialistName`, fallback localStorage legacy e banner specialistico derivato dai campi vecchi` | mitigation=`ripulire prima il transport server e poi i consumer client/UI per convergere su stateSnapshot come fonte primaria` | rollback=`mantenere i derived fields legacy finché i consumer UI non sono aggiornati` | stato=open
2026-03-26 23:55 | risk=R-CORE-007 | causa=`i payload text/live esponevano ancora campi ibridi legacy come sorgente primaria implicita` | sintomo=`domain/specialist fields potevano divergere dal lead panel canonico nello stream ui.state` | impatto=medium | detection=`inspection di chat/send/route.ts e test SSE mancanti sul lead panel canonico` | mitigation=`chat/send emette ora campi compat solo come derivati del lead panel canonico e i test bloccano regressioni` | rollback=`ripristinare il backup locale 2355_cp32-transport-ui-snapshot-first se i consumer legacy si rompessero` | stato=mitigated
2026-03-26 23:55 | risk=R-CORE-008 | causa=`i consumer client/UI usavano ancora fallback legacy come priorita logica rispetto allo snapshot` | sintomo=`ChatContext e ChatShell potevano mantenere uno stato visuale non allineato al panel canonico` | impatto=medium | detection=`inspection di ChatContext/ChatShell e assenza di sincronizzazione centralizzata del snapshot` | mitigation=`stateSnapshot esposto nel context e reso fonte primaria per dominio/specialista; ChatShell usa il canonico prima dei compat fields` | rollback=`ripristinare il backup locale 2355_cp32-transport-ui-snapshot-first se emergessero regressioni UI reali` | stato=mitigated
2026-03-26 23:55 | risk=R-CORE-009 | causa=`il protocol engine interno usa ancora una shape legacy come adapter per owner/protocol continuity` | sintomo=`il core non e` ancora completamente legacy-free anche se il canonico vince nei conflitti e nei boundary` | impatto=low | detection=`advanceCaseState() continua a lavorare su CaseState e l'orchestrator costruisce un adapter dal caseStateSnapshot` | mitigation=`trattare `CaseState` come adapter interno transitorio e non riaprire il protocol finche` non emerge un failure reale; tutti i boundary e consumer principali sono ora canonical-first` | rollback=`nessun rollback richiesto finche il comportamento canonical-first resta coperto dai test` | stato=accepted
2026-03-27 00:32 | risk=R-CORE-010 | causa=`il contract SSE shared non dichiarava esplicitamente ui.state.stateSnapshot e il client text inviava ancora activeSpecialistId anche se il server non lo usava piu`` | sintomo=`ambiguita` di tipo sul boundary text e rumore legacy nel request body` | impatto=low-medium | detection=`inspection di chatStream.ts, chat/send/route.ts e ChatContext.tsx durante cleanup track finale` | mitigation=`aggiunto stateSnapshot al tipo ChatStreamEvent.ui.state, unificata l'emissione con toSse(), rimosso activeSpecialistId dal request body client` | rollback=`ripristinare i file dal backup locale 0000_cp33-cleanup-review se emergessero regressioni SSE/client` | stato=mitigated
2026-03-27 00:32 | risk=R-CORE-011 | causa=`persistenza di codice morto legacy non piu` importato (`routingLegacy.ts`)` | sintomo=`rumore architetturale e falsa impressione di doppio sistema di routing ancora attivo` | impatto=low | detection=`ricerca riferimenti con rg su src/tests e assenza totale di import runtime` | mitigation=`rimosso il file morto e conservato backup locale dedicato prima della delete` | rollback=`ripristinare src/lib/ai/orchestrator/routingLegacy.ts dal backup locale 0031_cp33b-routinglegacy-cleanup` | stato=mitigated
2026-03-27 00:32 | risk=R-CORE-012 | causa=`restano derived compatibility fields e fallback storici locali per supportare consumer e record legacy` | sintomo=`il runtime e` coerente ma non completamente legacy-free nei bordi e nel protocol engine interno` | impatto=low | detection=`inspection finale di ChatContext, chat/send, fastPaths, orchestrator e persistence` | mitigation=`accettare il residuo finche non emergono evidenze che sia davvero inutile; i path canonici restano prioritari e testati` | rollback=`nessun rollback richiesto: e` un residuo consapevole, non un rischio aperto ad alto impatto` | stato=accepted
2026-03-27 08:16 | risk=R-INFRA-004 | causa=`Next 16 continua a segnalare warning su workspace root multipli e deprecazione middleware->proxy anche se il runtime shared e` corretto` | sintomo=`build production verde con warning non bloccanti` | impatto=low | detection=`npm run build locale e log build Vercel del deploy finale` | mitigation=`trattare il warning come follow-up infrastrutturale separato, senza riaprire il cleanup architetturale shared text/live` | rollback=`nessun rollback richiesto; il deploy finale e` sano e verificato` | stato=accepted
2026-03-27 10:35 | risk=R-INFRA-005 | causa=`next@16.1.6 esponeva advisory moderate note e la build Next segnalava warning concreti su middleware deprecato e workspace root multipli` | sintomo=`rischio runtime/framework non nullo e rumore ripetuto in build/deploy` | impatto=medium | detection=`npm audit`, log build locale/Vercel e warning espliciti di Next 16` | mitigation=`upgrade a next/eslint-config-next 16.2.1, migrazione src/middleware.ts -> src/proxy.ts, aggiunta di turbopack.root in next.config.ts` | rollback=`ripristinare i file dal backup locale 0818_cp35-infra-security-track e redeploy del commit 1a92b30` | stato=mitigated
2026-03-27 10:35 | risk=R-INFRA-006 | causa=`parte dell'audit residuo vive nel solo dev-toolchain (`picomatch` via vitest/lint-staged; `minimatch` via eslint stack) e richiede major upgrade o override potenzialmente invasivi` | sintomo=`npm audit resta a 10 vulnerabilita` (`9 moderate`, `1 high`) anche dopo i fix patch/minor e gli overrides sicuri` | impatto=medium | detection=`npm audit --json` post-fix + inspection dell'albero dipendenze con npm ls` | mitigation=`accettare come residuo non runtime: nessuna reachability applicativa/production confermata; rinviare a una futura track dedicata di major toolchain upgrades` | rollback=`nessun rollback richiesto: non e` un fix parziale instabile, ma un residuo deliberatamente non forzato` | stato=accepted
2026-03-27 10:35 | risk=R-INFRA-007 | causa=`overrides transitive mal calibrati avrebbero potuto introdurre regressioni build/runtime in librerie shared come google-auth-library o vite/vitest` | sintomo=`possibili break non ovvi dopo npm install` | impatto=medium | detection=`typecheck/build/test mirati + deploy production dopo apply degli overrides` | mitigation=`applicati solo override a patch/minor con beneficio concreto (`gaxios`, `google-auth-library`, `flatted`, `yaml`); evitato override globale di picomatch per non rompere micromatch/lint-staged` | rollback=`ripristinare package.json e package-lock.json dal backup locale CP-35 e rieseguire npm install` | stato=mitigated
2026-03-27 10:40 | id=R-TOOLCHAIN-37 | severita=medium | stato=mitigated | rischio=`vitest@4` puo` rompere mocking storico e generare regressioni test-only | evidenza=failure iniziale su `tests/api/live-token-security.test.ts` con `POST` importato prima dei mock e warning `vi.fn()` | mitigazione=adattare i test live-token a mock constructor-style e import lazy del route | rollback=restaurare backup `1052_cp38-vitest4-test-fix` o declassare `vitest@4` a future upgrade
2026-03-27 10:55 | id=R-TOOLCHAIN-38 | severita=medium | stato=accepted-residual | rischio=restano `9` advisory moderate sulla stack lint/eslint/minimatch | evidenza=`npm audit --json` post-track: solo `eslint`, `eslint-config-next`, `eslint-plugin-import`, `minimatch`, `brace-expansion` | mitigazione=nessun runtime impact; pianificare track separata solo se il ROI di `eslint@10` diventa favorevole | rollback=non necessario, residuo accettato
2026-03-27 11:24 | id=R-TOOLCHAIN-39 | severita=low | stato=accepted-residual | rischio=warning/deprecazioni transitive Vercel (`whatwg-encoding`, `node-domexception`) e major Prisma ancora presenti nei log deploy | evidenza=output `npx vercel --prod --yes` e advisory di update Prisma `5.22.0 -> 7.5.0` | mitigazione=nessun impatto runtime osservato; tenere come raccomandazione futura separata | rollback=non necessario
2026-03-27 12:08 | id=R-REVIEW-040 | severita=high | stato=confirmed | rischio=`chat/send/route.ts` resta semanticamente single-agent per l'esecuzione tool mentre `live-sync` seleziona l'agente per-call dal panel/domain context | evidenza=`const capabilityAgentId = consensus.activeSpecialist?.id ?? ...` in chat/send contro selezione `callPanel/liveLeadPanel` in live-sync; test verdi non coprono l'allineamento tra i due canali | mitigazione=aprire una track correttiva per allineare anche il route text alla selezione tool per-call/panel-aware | rollback=nessun rollback; rischio da correggere nel prossimo step dedicato
2026-03-27 12:08 | id=R-REVIEW-041 | severita=medium | stato=confirmed | rischio=`readCanonicalCaseRuntimeState()` puo` accettare snapshot canonici parziali o corrotti grazie a default permissivi di `normalizeCaseState()` | evidenza=`parseStoredCanonicalSnapshot()` costruisce placeholder legacy e passa da `normalizeCaseState()`, che completa `domainPanels`, `activeDomains`, `speakerPolicy`, `conversationFocus`, `coordinationState`; i test coprono l'assenza totale dello snapshot, non il malformed partial snapshot | mitigazione=stringere la validazione del boundary canonico e aggiungere test negativi su snapshot parziali/incoerenti | rollback=nessun rollback; rischio da correggere con hardening del boundary
2026-03-27 12:08 | id=R-REVIEW-042 | severita=medium | stato=confirmed | rischio=adapter legacy ancora vivi nel path caldo dei route principali possono mascherare divergenze contrattuali tra canonico e legacy | evidenza=`applyCanonicalSnapshotToLegacyCaseState()` e `getCaseState()` sono ancora usati in chat/send, live-sync e chatPersistence; `fastPaths.ts` ricostruisce un `compatibilitySpeakerId` da snapshot/legacy | mitigazione=ridurre il path legacy a casi storici freddi e testare l'assenza di dipendenze hot-path dal compat layer | rollback=non applicabile; residuo strutturale da affrontare
2026-03-27 12:08 | id=R-REVIEW-043 | severita=medium | stato=confirmed | rischio=il bootstrap/sicurezza live e` coperto soprattutto da test mock-heavy, quindi regressioni reali di integrazione possono passare inosservate | evidenza=`tests/api/live-token-security.test.ts` e `tests/api/live-token-fallback-observability.test.ts` mockano Prisma e `@google/genai`; nessun test esercita SDK/DB reali o il proxy/framework boundary | mitigazione=aggiungere almeno una verifica di integrazione piu` realistica o dichiarare esplicitamente il gap come track obbligatoria di affidabilita` live | rollback=non applicabile
2026-03-27 12:53 | id=R-REVIEW-040 | severita=high | stato=mitigated | rischio=`chat/send/route.ts` restava semanticamente single-agent per i tool mentre `live-sync` era per-call panel-aware | evidenza=`chat/send` usava un solo `capabilityAgentId` per il turno; ora sia text che live passano da `resolveToolExecutionAgent()` con risoluzione per-call dal panel/domain context | mitigazione=`src/lib/tools/toolExecutionRouting.ts` condiviso tra `chat/send` e `live-sync`, test route text aggiunto su due tool call con agenti diversi per dominio | rollback=`ripristinare i file dal backup locale 1215_cp41-mandatory-tracks-start o revert del commit correttivo se emergono regressioni cross-tool`
2026-03-27 12:53 | id=R-REVIEW-041 | severita=medium | stato=mitigated | rischio=`readCanonicalCaseRuntimeState()` accettava snapshot parziali o corrotti con default permissivi | evidenza=`parseStoredCanonicalSnapshot()` ora valida con schema stretto e `fromStoredCaseState()` non degrada piu` a legacy se uno snapshot malformed e` presente | mitigazione=`zod` strict schema nel boundary canonico + test negativi su snapshot malformed con legacy confliggente | rollback=`ripristinare src/lib/ai/case/persistence.ts e tests/api/case-persistence.test.ts dal backup locale 1215_cp41-mandatory-tracks-start`
2026-03-27 12:53 | id=R-REVIEW-042 | severita=medium | stato=partially-mitigated | rischio=adapter legacy ancora vivi nel path caldo dei route principali | evidenza=`chat/send` e `live-sync` non ricostruiscono piu` `CaseState` quando `stateSnapshot` esiste, ma `orchestrator.ts`, `fastPaths.ts` e le facade compat in `chatPersistence.ts` usano ancora adapter legacy per il protocol engine interno e per compatibilita` storica | mitigazione=confinato il legacy fuori dai route hot-path principali; resta residuo interno non piu` guida operativa del runtime | rollback=non necessario; residuo accettabile finche il protocol engine interno non verra` separato in una track dedicata
2026-03-27 12:53 | id=R-REVIEW-043 | severita=medium | stato=accepted-residual | rischio=il bootstrap/sicurezza live resta coperto soprattutto da test mock-heavy | evidenza=la track corrente non modifica `live-token`/proxy/framework boundary; restano verdi i test esistenti ma manca un check di integrazione meno mockato | mitigazione=accettato come residuo non blocking/high; da trattare solo se si apre una track di affidabilita` live/integration testing | rollback=non applicabile
2026-03-27 15:26 | id=R-RECON-044 | severita=high | stato=confirmed | rischio=il cuore multi-dominio resta solo parzialmente riconciliato con la richiesta storica `context-first / LLM-first` perche` il routing e la preselezione dominio dipendono ancora in modo strutturale da keyword e hints statici | evidenza=`src/lib/ai/domain/domainDetection.ts` usa `KEYWORDS`, `src/lib/ai/orchestrator/routing.ts` usa `SPECIALIST_KEYWORDS`, `src/lib/ai/orchestrator/agentSelection.ts` usa `AGENT_COMPETENCE_HINTS`; `tests/api/multi-agent-routing.test.ts` verifica esplicitamente il path euristico | mitigazione=serve una track dedicata di routing multi-dominio che sposti il cuore della selezione da euristiche statiche a ragionamento/context scoring piu` ricco, con test di regressione che provino la non-dipendenza dal trigger matching | rollback=non applicabile; nessun fix locale minimo puo` chiudere questo gap senza una track mirata
2026-03-27 16:16 | id=R-RECON-044 | severita=high | stato=mitigated | rischio=il routing multi-dominio restava keyword-heavy nel cuore del flusso | evidenza=`orchestrator.ts` ora usa `resolveContextualRouting()` e `inferRoutingWithLlm()` prima delle euristiche; `case/protocol.ts` preserva lo speaker/panel corrente nei follow-up contestuali; `agentSelection.ts` tratta `AGENT_COMPETENCE_HINTS` come booster e `preferredAgentIds` ordinati come segnale primario; test verdi su `chat-orchestration`, `multi-agent-routing`, `orchestrator-routing-phase1`, `case-protocol`, `multi-agent-protocol`` | mitigazione=le keyword restano supporto/fallback in `domainDetection.ts` e `routing.ts`, ma non sono piu` il motore principale del path reale orchestrator/routing | rollback=`ripristinare i file dal backup locale bkp/backups/2026-03-27/1618_context-first-routing-closeout o revert del commit applicativo se emergono regressioni`
2026-03-27 16:16 | id=R-UX-046 | severita=low | stato=mitigated | rischio=warning lint/framework locale su `UserAvatar.tsx` nascondeva residui minori e rumore nella baseline finale | evidenza=`eslint` segnalava `@next/next/no-img-element` su `src/components/profile/UserAvatar.tsx` | mitigazione=`img` sostituita con `next/image` in modalita` `unoptimized` per mantenere compatibilita` con URL dinamici e chiudere il warning senza cambiare UX | rollback=`ripristinare src/components/profile/UserAvatar.tsx dal backup 1618_context-first-routing-closeout`
2026-03-27 16:49 | id=R-RESID-048 | severita=medium | stato=mitigated | rischio=i route principali ricostruivano ancora `CaseState` solo per serializzare poi di nuovo `stateSnapshot`, lasciando bridge legacy non necessario nel path di persistenza normale | evidenza=`chat/send/route.ts`, `live-sync/route.ts` e `chatPersistence.ts` ora persistono `CanonicalCaseRuntimeState` come payload primario e il writer snapshot-first deriva solo le colonne legacy minime richieste dallo schema | mitigazione=`toStoredCaseRuntimeState()` in `persistence.ts` + uso esplicito di `persistCaseRuntimeState()` nei route snapshot-first | rollback=`ripristinare i file dal backup locale bkp/backups/2026-03-27/1700_residual-cleanup o revert del commit della track residua`
2026-03-27 16:49 | id=R-RESID-049 | severita=medium | stato=partially-mitigated | rischio=i test live restavano troppo superficiali sul bootstrap reale e sul contenuto del system instruction | evidenza=aggiunti guardrail su `live-token` per latest user snapshot senza conversationId, panel summary, attributi e cronologia recente; resta comunque assente una integrazione browser/SDK reale non mockata | mitigazione=rafforzati `tests/api/live-token-security.test.ts` e `tests/api/live-token-fallback-observability.test.ts` sui path reali osservabili del route; residuo accettato solo sul livello di integrazione esterna | rollback=non necessario; se emergono regressioni live reali aprire track separata di integrazione end-to-end
2026-03-27 20:43 | id=R-FEEDBACK-050 | severita=high | stato=mitigated | rischio=nel live transcript l'assistant poteva comparire prima dell'utente per race tra persist locale del messaggio user e persist del messaggio assistant successivo | evidenza=`ChatInput.tsx` chiamava il transcript route per user e assistant senza serializzazione condivisa; il test reale utente mostrava risposta assistant sopra a `va bene`; aggiunto guardrail in `tests/chat-input-live-ordering.test.tsx` | mitigazione=queue client-side delle write transcript, append solo dei messaggi realmente salvati dal route e persistenza assistant dopo sync live e completamento della write user | rollback=`git revert f282175` o ripristino del backup `bkp/backups/2026-03-27/2030_feedback-fix-pack` se emergono regressioni nuove sul live transcript
2026-03-27 20:43 | id=R-FEEDBACK-051 | severita=high | stato=mitigated | rischio=payload/tool interni (`Payload: user.setAttribute ...`) trapelavano nell'output utente, nel load conversazione e nell'export txt | evidenza=feedback utente reale + nuovo test `tests/api/conversation-thinking-export.test.ts`; i route transcript/load/export/list mostravano il contenuto assistant raw senza filtro dedicato | mitigazione=introdotto `sanitizeAssistantVisibleContent()` e riuso nei route `chat/transcript`, `conversations/[id]`, `conversations/[id]/export`, `conversations`; i messaggi assistant vuoti dopo sanitizzazione vengono scartati | rollback=`git revert f282175` o ripristino del backup `bkp/backups/2026-03-27/2030_feedback-fix-pack`
2026-03-27 20:43 | id=R-FEEDBACK-052 | severita=high | stato=mitigated | rischio=la UI mostrava banner/label di uno specialista (`MMG`) mentre il testo del messaggio e il path runtime facevano parlare un altro speaker reale (`Fisioterapista`) | evidenza=screenshot utente + nuovi guardrail in `tests/api/chat-send-persistence.test.ts` e `tests/chat-shell-specialist-banner.test.tsx`; la label del messaggio e il banner derivavano ancora dal lead panel globale in alcuni path | mitigazione=`chat/send` fa vincere la label dello speaker corrente; `ChatContext` conserva `domain/specialistName` del messaggio assistant; `ChatShell` usa l'ultimo assistant confermato come sorgente primaria del banner | rollback=`git revert f282175` o ripristino del backup `bkp/backups/2026-03-27/2030_feedback-fix-pack`
2026-03-27 20:43 | id=R-FEEDBACK-053 | severita=medium | stato=accepted-residual | rischio=su chiusure live estremamente rapide resta possibile una finestra stretta in cui il transcript client non completa l'ultimo flush prima del teardown della sessione | evidenza=la serializzazione client riduce il problema ma non puo` garantire da terminale il comportamento browser in close abrupt immediato; nessun failure riprodotto nei test mirati locali | mitigazione=il path normale e` ora serializzato e validated; monitorare feedback production e aprire solo se compaiono evidenze reali di messaggi persi a fine sessione | rollback=non necessario; il rischio residuo non giustifica ulteriore refactor in questo pack
2026-03-27 20:54 | id=R-QA-054 | severita=medium | stato=mitigated | rischio=il cluster filtering era chiuso contro il leak di `Payload:`, ma il sanitizer/transcript path poteva scartare interamente un assistant transcript che conteneva sia una riga interna sia testo visibile legittimo | evidenza=prima della fix la POST `/api/chat/transcript` restituiva `savedMessages: []` sul caso mixed-content; dopo il deploy `fcf51b9` la stessa prova production salva `Ti aiuto a impostare il percorso.` nella conversazione `cmn9dpqha0001lrqwlmmsieis`, senza `Payload:` in load/export | mitigazione=`sanitizeAssistantVisibleContent()` ora rimuove payload JSON inline e sequenze legacy `key:\"value\"` dopo la directive interna, preservando il testo assistant residuo sulla stessa riga | rollback=`git revert fcf51b9` e nuovo deploy della baseline precedente se emergono regressioni inattese nel filtering transcript`
2026-03-27 21:56 | id=R-QA-055 | severita=low | stato=mitigated | rischio=la chiusura del fix pack poteva restare non confermata senza una rerun production fresca post-deploy | evidenza=rerun QA production completata con transcript route sulla conversazione `cmn9e82do0001oc3sfh4i5q0o` e chat/send sulla conversazione `cmn9e8ho40005oc3svaq2ilaw`; tutti e quattro i controlli richiesti risultano PASS | mitigazione=nuove evidenze raccolte su sessione smoke reale, transcript, export e SSE production | rollback=non applicabile; entry di conferma QA
2026-03-27 22:20 | id=R-LIVE-056 | severita=medium | stato=mitigated | rischio=il perimetro live aveva ancora una copertura troppo debole sul contratto browser-facing tra `LiveModal`, `/api/live-token`, snapshot bootstrap e config SDK, e mancavano guardrail espliciti sulle continuity sources `snapshot_context`/`history_context` | evidenza=prima di questa track non esistevano test dedicati su `LiveModal`; i source `snapshot_context` e `history_context` erano presenti nel codice ma senza test mirati locali | mitigazione=aggiunti `tests/api/live-modal-bootstrap.test.ts` e `tests/api/contextual-routing.test.ts`; i test bloccano regressioni su fetch del token live, fallback a snapshot client-side, persistenza del `stateSnapshot` server e priorita` di `snapshot_context/history_context` rispetto al fallback euristico | rollback=`git reset --hard backup/2026-03-27-2230_live-integration-guardrails` se i nuovi guardrail rivelassero regressioni future da isolare`
2026-03-27 22:40 | id=R-UI-057 | severita=high | stato=mitigated | rischio=la UI chat aveva perso il comportamento "vivo" dei domini dopo il passaggio a multi-dominio: i pulsanti della barra in basso non si accendevano piu` in modo coerente e il dominio corrente poteva restare bloccato su metadata stale dell'ultimo assistant | evidenza=`ChatShell.tsx` privilegiava `latestAssistantWithDomain` rispetto a `activeDomain/stateSnapshot`; `ChatInput.tsx` supportava un solo `activeDomain`; screenshot utente con banner specialistico alto ma nessun segnale dominio vivo in basso | mitigazione=`ChatShell` ora fa vincere il dominio canonico corrente, `ChatInput` evidenzia tutti i domini attivi e switcha sul focus corrente, il banner alto e` stato rimosso, e nuovi guardrail bloccano regressioni su highlight/switch/colorazione bubble | rollback=`ripristinare i file dal branch backup/2026-03-27-2135_domain-ui-regression-fix o dal backup locale bkp/backups/2026-03-27/2135_domain-ui-regression-fix`
2026-03-27 22:59 | id=R-QA-058 | severita=high | stato=open | rischio=il fix UI domini/chat e` corretto localmente ma in production non si attiva perche` il backend continua a serializzare i casi mono/multi-dominio come `general` con `activeDomains=[]` | evidenza=smoke production autenticato sulla conversazione `cmn9g02d30014128naqhn91jr`: prompt `Mi serve una scheda per ricominciare ad allenarmi in palestra.` produce `ui.state.domain=general`, `stateSnapshot.leadDomain=general`, `activeDomains=[]`, assistant persistito `domain=general`; follow-up `dolore al ginocchio ... senza perdere il focus sull'allenamento` mantiene ancora `general` e `activeDomains=[]` | mitigazione=aprire fix backend stretto sui moduli che calcolano/serializzano `ui.state.domain`, `stateSnapshot.leadDomain/activeDomains` e `message.domain` nel path production text chat | rollback=`non applicabile; finding QA su production`
2026-03-27 23:08 | id=R-QA-058 | severita=high | stato=mitigated | rischio=il fix UI domini/chat era bloccato dal backend che continuava a serializzare casi mono/multi-dominio come `general` | evidenza=nuovo patch locale in `src/lib/ai/orchestrator/orchestrator.ts` con guardrail verdi su snapshot `training` e `health+training`; route persistence test ora blocca anche `assistant.domain` allineato al `leadDomain` canonico | mitigazione=il `stateSnapshot` viene ora costruito con domini e pannelli coerenti prima della serializzazione production-facing; resta da confermare la smoke production post-deploy sugli stessi scenari QA | rollback=`ripristinare il backup locale bkp/backups/2026-03-27/2302_domain-state-backend-fix o revert del commit applicativo se il deploy mostra regressioni`
