Worklog (append-only)

2026-03-27 22:20 | ruolo=backend-developer | fatto=chiusa la track separata live/browser meno mock-heavy con guardrail browser-facing e continuity sources | output=creato checkpoint Git `backup/2026-03-27-2230_live-integration-guardrails` e backup locale `bkp/backups/2026-03-27/2230_live-integration-guardrails`; aggiunti `tests/api/live-modal-bootstrap.test.ts` sul boundary `LiveModal -> /api/live-token -> GoogleGenAI.live.connect` e `tests/api/contextual-routing.test.ts` sui source `snapshot_context/history_context`; validazioni verdi con suite live mirata, `npm run typecheck`, `npm run lint`, `npm run build` | prossimo=commit, push e deploy del commit test-only; poi chiudere con soli residual recommendations fuori scope

2026-03-27 20:43 | ruolo=backend-developer | fatto=implementato fix pack prioritario post-feedback reale su transcript live, payload leakage e coerenza specialista/UI | output=creato backup locale `bkp/backups/2026-03-27/2030_feedback-fix-pack`; introdotto `src/lib/chat/userVisibleContent.ts`; `src/app/api/chat/transcript/route.ts` ora sanifica e persiste in ordine con metadata assistant; `src/components/chat/ChatInput.tsx` serializza i save live e allinea il transcript assistant allo snapshot post-`live-sync`; `src/app/api/conversations/[id]/route.ts`, `src/app/api/conversations/[id]/export/route.ts` e `src/app/api/conversations/route.ts` filtrano i payload interni; `src/components/chat/ChatShell.tsx` usa la label dell'ultimo speaker assistant per il banner; test nuovi su transcript route, ordering live e banner specialistico; verdi `npm run typecheck`, `npm run lint`, `npm run build` e suite mirate | prossimo=eseguire commit, push e deploy del fix pack e poi verificare l'alias production

2026-03-27 20:28 | ruolo=backend-developer | fatto=analisi runtime del primo test reale utente con screenshot e export feedback | output=confermati tre cluster ad alta priorita`: ordering errato dei messaggi live nel transcript, leakage di payload/tool interni nell'output utente e mismatch tra specialista visualizzato e speaker reale; individuati come file piu` probabili del fix pack `src/app/api/chat/transcript/route.ts`, `src/components/chat/ChatInput.tsx`, `src/components/chat/live/LiveModal.tsx`, `src/contexts/ChatContext.tsx`, `src/components/chat/ChatShell.tsx`, `src/components/chat/MessageBubble.tsx`, `src/app/api/chat/send/chatStream.ts`, `src/app/api/conversations/[id]/export/route.ts` | prossimo=consegnare report operativo e, se confermato, implementare i fix prioritari con test mirati

2026-03-26 23:45 | ruolo=backend-developer | fatto=migrazione di src/app/api/chat/live-sync/route.ts al boundary canonico con tool context panel-aware | output=creato backup locale `bkp/backups/2026-03-26/2346_cp31-live-sync-canonical`; `live-sync` usa ora `getCaseRuntimeState()` come primary read path, persiste canonical-first e seleziona l'agent tool per-call dal panel/domain context invece di usare un solo `capabilityAgentId`; aggiunto test mirato in `tests/api/live-sync-stateSnapshot.test.ts`; validazioni verdi con `npm run test -- tests/api/live-sync-stateSnapshot.test.ts tests/api/conversation-stateSnapshot-route.test.ts tests/api/live-token-fallback-observability.test.ts tests/api/live-token-security.test.ts tests/api/chat-send-persistence.test.ts tests/api/case-persistence.test.ts` e `npm run typecheck` | prossimo=passare al cleanup del contratto server snapshot-first e poi dei consumer client/UI

2026-03-26 23:42 | ruolo=backend-developer | fatto=migrazione di src/app/api/conversations/[id]/route.ts al boundary canonico | output=creato backup locale `bkp/backups/2026-03-26/2344_cp30-conversations-route-canonical`; il reload conversazione legge ora `readCanonicalCaseRuntimeState()` come source of truth del payload `stateSnapshot`; validazioni verdi con `npm run test -- tests/api/conversation-stateSnapshot-route.test.ts tests/api/live-token-fallback-observability.test.ts tests/api/live-token-security.test.ts tests/api/chat-send-persistence.test.ts tests/api/case-persistence.test.ts` e `npm run typecheck` | prossimo=migrare `src/app/api/chat/live-sync/route.ts` al boundary canonico e poi ridurne la semantica mono-agent

2026-03-26 23:40 | ruolo=backend-developer | fatto=migrazione di src/app/api/live-token/route.ts al boundary canonico | output=creato backup locale `bkp/backups/2026-03-26/2342_cp29-live-token-canonical`; il bootstrap live usa ora `readCanonicalCaseRuntimeState()` come primary path e risolve `activeAgentId` dal lead panel canonico; aggiunto test mirato in `tests/api/live-token-fallback-observability.test.ts`; validazioni verdi con `npm run test -- tests/api/live-token-fallback-observability.test.ts tests/api/live-token-security.test.ts tests/api/chat-send-persistence.test.ts tests/api/case-persistence.test.ts` e `npm run typecheck` | prossimo=migrare `src/app/api/conversations/[id]/route.ts` al boundary canonico

2026-03-26 23:38 | ruolo=backend-developer | fatto=migrazione di src/app/api/chat/send/route.ts al boundary canonico | output=creato backup locale `bkp/backups/2026-03-26/2350_cp28-send-route-canonical`; il route text usa ora `getCaseRuntimeState()` come primary read path e passa `caseStateSnapshot` all'orchestrator; la persistenza usa `persistCaseState()` quando esiste `nextCaseState` e `persistCaseRuntimeState()` solo come fallback runtime; aggiunto test mirato sul passaggio di `caseStateSnapshot`; validazioni verdi con `npm run test -- tests/api/chat-send-persistence.test.ts tests/api/case-persistence.test.ts` e `npm run typecheck` | prossimo=migrare `src/app/api/live-token/route.ts` al nuovo read path canonico

2026-03-26 23:35 | ruolo=backend-developer | fatto=migrazione del primo consumer canonical-first in src/app/api/chat/send/chatPersistence.ts | output=creato backup locale `bkp/backups/2026-03-26/2345_cp27-chatpersistence-canonical`; `RoutePersistenceDeps` ora espone `getCaseRuntimeState()` e `persistCaseRuntimeState()`; `persistCaseState()` converge sul writer canonico quando possibile senza rompere i caller legacy; estesi i test in `tests/api/chat-send-persistence.test.ts`; validazioni verdi con `npm run test -- tests/api/chat-send-persistence.test.ts tests/api/case-persistence.test.ts` e `npm run typecheck` | prossimo=migrare `src/app/api/chat/send/route.ts` al nuovo boundary canonico

2026-03-26 23:20 | ruolo=backend-developer | fatto=refactor stretto del boundary canonical-first in persistence.ts | output=creato backup locale `bkp/backups/2026-03-26/2315_persistence-boundary-refactor`; introdotto `readCanonicalCaseRuntimeState()` come primary read path; isolati parser snapshot canonico e parser legacy; `fromStoredCaseState()` ridotta a facade legacy-safe; `tests/api/case-persistence.test.ts` esteso a 6 casi minimi; validazioni verdi con `npm run test -- tests/api/case-persistence.test.ts` e `npm run typecheck` | prossimo=attendere eventuale task successivo; primo consumer corretto da migrare e` `src/app/api/chat/send/chatPersistence.ts`

2026-03-26 23:07 | ruolo=backend-developer | fatto=fix minimo del warning residuo GitHub Actions Node 20 in CI | output=creato backup locale `bkp/backups/2026-03-26/2300_ci-node24-warning`; `.github/workflows/ci.yml` aggiornato da `actions/checkout@v4` a `@v5` e da `actions/setup-node@v4` a `@v5`; validazione minima verde con `git diff --check HEAD -- .github/workflows/ci.yml` e `npx prettier --check .github/workflows/ci.yml` | prossimo=commit mirato, push su `main` e verifica del nuovo run CI senza warning di runtime deprecato

2026-03-26 22:55 | ruolo=backend-developer | fatto=fix locale dei failure CI piu recenti su main con perimetro solo test | output=creato backup locale `bkp/backups/2026-03-26/2254_ci-fix-tests`; aggiunto `prisma.caseState.findUnique` al mock di `tests/api/conversation-thinking-export.test.ts`; riallineato `tests/api/orchestrator-interview-flow.test.ts` ai peer stub proposals attuali; validazioni verdi con `npm run test -- tests/api/conversation-thinking-export.test.ts tests/api/orchestrator-interview-flow.test.ts`, `npm run test -- tests/api/conversation-stateSnapshot-route.test.ts tests/api/case-persistence.test.ts` e `npm run typecheck` | prossimo=commit mirato, push su `main` e verifica del nuovo workflow CI

2026-03-26 21:30 | ruolo=code-reviewer | fatto=classificazione finale pulita dei residui post-Fase 1 shared text/live | output=identificati tre residui non bloccanti ma reali: proiezione del canonico ancora mediata da shape legacy nel core `CaseState`, esecuzione tool live ancora mono-agent per turno, consumer/SSE ancora ibridi con `activeSpecialist*`; classificazione finale pronta per fase successiva senza nuove implementazioni | prossimo=attendere conferma utente; eventuale follow-up solo su cleanup legacy o piano Fase successiva

2026-03-26 21:24 | ruolo=backend-developer | fatto=pubblicazione remota finale della chiusura Fase 1 | output=`npm run build` verde; commit `44231c5` creato e pushato su `origin/main`; deploy Vercel production completato su `https://livewell.mottisi.com`; verifica HTTP positiva con fetch della homepage deployata | prossimo=nessun passo obbligatorio; Fase 1 shared text/live chiusa

2026-03-26 21:17 | ruolo=backend-developer | fatto=chiusura reale Fase 1 shared text/live con persistenza canonica e campagna finale formalizzata | output=risolte incoerenze storiche Prisma con `migrate resolve`, applicata la migrazione `20260326205200_add_case_state_snapshot`, aggiunti test finali su reload conversazione e live-sync canonical-first; suite finale minima verde (`typecheck` + 8 file di test mirati) e checkpoint locale `bkp/backups/2026-03-26/2117_cp23-step23-final-campaign` | prossimo=nessun passo Fase 1 obbligatorio; eventuale follow-up solo per cleanup legacy in fase successiva

2026-03-26 20:56 | ruolo=backend-developer | fatto=downsizing reale del compat layer nel read path Fase 1 | output=creato snapshot locale `bkp/backups/2026-03-26/2102_cp22-step22-canonical-readpath`; `src/lib/ai/case/persistence.ts` ora privilegia `stateSnapshot` e usa il legacy solo come bridge/fallback; aggiunto `tests/api/case-persistence.test.ts`; validazione verde con `npm run typecheck` e suite mirata (case-persistence, chat-send-persistence, live-token fallback/security)` | prossimo=continuare automaticamente con la campagna test finale Fase 1 text/live/reload
2026-03-26 20:53 | ruolo=backend-developer | fatto=esecuzione reale del primo blocco finale di persistenza canonica Fase 1 | output=creato snapshot locale `bkp/backups/2026-03-26/2052_cp21-step21-canonical-persistence`; aggiunto campo `stateSnapshot` persistito a `CaseState` in Prisma con migrazione SQL minima; `chatPersistence.ts` ora esegue il dual-write reale del canonico; `live-token/route.ts` usa `fromStoredCaseState()` invece del mapper row legacy manuale; validazione verde con `npx prisma generate`, `npm run typecheck` e `tests/api/chat-send-persistence.test.ts` | prossimo=continuare automaticamente sul downsizing del compat layer nel read path canonico

2026-03-26 20:24 | ruolo=qa-expert | fatto=verifica locale mirata post-fix dei due rischi prestazionali minimi | output=smoke autenticato locale completato con successo su `http://127.0.0.1:3000`; il primo `POST /api/chat/send` e` rientrato nel timeout client (`200 in 29.1s`) e `/tmp/livewell_chat1_1774553008.sse` contiene `message.complete`; nei log runtime del run non compaiono piu timeout `Round 2`, ma solo timeout singoli di `Round 1` su agenti lenti; evidenze aggregate in `/tmp/livewell_smoke_summary_1774553008.json` | prossimo=attendere conferma utente; se richiesto, decidere se fermarsi qui o aprire un follow-up solo sui rischi prestazionali residui

2026-03-26 20:20 | ruolo=backend-developer | fatto=esecuzione reale del secondo fix minimo prestazionale sui timeout agentici duplicati | output=creato snapshot locale `bkp/backups/2026-03-26/2020_cp20-step20-agent-round-timeout`; `src/lib/ai/orchestrator/agentRoundExecution.ts` ora evita di rieseguire nel round 2 gli agenti gia degradati a fallback (`confidence=0`) nel round 1, riducendo il costo cumulativo dei timeout senza cambiare il contratto della pipeline; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, rieseguire una verifica mirata della latenza locale post-fix

2026-03-26 20:15 | ruolo=backend-developer | fatto=esecuzione reale del primo fix minimo prestazionale lato client su `chat/send` | output=creato snapshot locale `bkp/backups/2026-03-26/2015_cp19-step19-chatcontext-timeout`; `src/contexts/ChatContext.tsx` ora tenta un recovery server-side della conversazione dopo un fallimento stream non abortito, prima di mostrare l'errore locale, riducendo i falsi timeout client sul primo turno; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, passare al secondo fix minimo su `src/lib/ai/orchestrator/agentRoundExecution.ts`

2026-03-26 20:12 | ruolo=code-reviewer | fatto=review mirata del rischio prestazionale locale emerso nello smoke su `chat/send` e timeout agentici | output=confermati due finding reali: il primo `POST /api/chat/send` locale puo superare il timeout client di 30s pur completando lato server; i timeout agentici a 8s per round sono intenzionali e testati, ma su agenti lenti raddoppiano il costo del turno perche il medesimo agente puo scadere sia in round 1 sia in round 2; nessun crash confermato, impatto primario su latenza percepita e smoke false-negative | prossimo=attendere conferma utente; se richiesto, valutare solo il perimetro minimo di mitigazione prestazionale senza refactor nuovi

2026-03-26 19:24 | ruolo=qa-expert | fatto=verifica runtime mirata locale dei payload `stateSnapshot` dopo il fix su `chat/send` | output=smoke autenticato reale su `http://127.0.0.1:3000` con sessione `smoke_1774548894@example.com`; `GET /api/conversations/smokeconv_1774548894` restituisce ora `stateSnapshot` nel payload JSON; `POST /api/chat/live-sync` restituisce `{\"ok\":true,\"stateSnapshot\":...}` nello stesso caso reale; durante lo smoke completo il primo `chat/send` e` andato oltre i 30s del client ma il server ha comunque completato i turni (`POST /api/chat/send 200`, `POST /api/chat/live-sync 200`) | prossimo=attendere conferma utente; se richiesto, eseguire un nuovo review/smoke solo sui rischi runtime residui

2026-03-26 19:04 | ruolo=backend-developer | fatto=esecuzione reale del perimetro minimo sul producer `chat/send` per il gap runtime `stateSnapshot` | output=creato snapshot locale `bkp/backups/2026-03-26/1908_cp18-step18-send-case-state`; `src/app/api/chat/send/route.ts` ora materializza `nextCaseState` anche da `consensus.stateSnapshot`, lo persiste come `CaseState` legacy e usa fallback `storedCaseState` per emettere `stateSnapshot`; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, rieseguire verifica mirata sui payload reali di `/api/conversations/[id]` e `/api/chat/live-sync`

2026-03-26 19:02 | ruolo=backend-developer | fatto=analisi tecnica dei gap runtime emersi nello smoke production su `stateSnapshot` | output=individuato che sia `/api/conversations/[id]` sia `/api/chat/live-sync` restituiscono `stateSnapshot` solo se esiste un `CaseState` ricostruibile; la persistenza corrente salva ancora solo la shape legacy e `chat/send/route.ts` persiste `CaseState` solo quando `consensus.caseState` e` presente, quindi il gap minimo da spiegare/correggere coinvolge route consumer + producer di `CaseState` + persistence legacy-safe, non UI o Prisma in prima battuta | prossimo=attendere conferma utente; se richiesto, implementare il perimetro minimo partendo dal producer `chat/send/route.ts`

2026-03-26 18:42 | ruolo=qa-expert | fatto=smoke autenticato production del perimetro text/live aggiornato | output=registrato utente smoke reale e sessione autenticata; `GET /api/conversations/[id]` restituisce cronologia e thinking persistito, ma nessun campo `stateSnapshot` nel payload reale osservato; `POST /api/live-token` restituisce bootstrap live coerente con `systemInstruction` e contesto specialistico; `POST /api/chat/live-sync` risponde `{\"ok\":true}` senza `stateSnapshot` nel caso provato, quindi l'aggiornamento client via response non e garantito su ogni turno | prossimo=attendere conferma utente; se richiesto, consegnare perimetro minimo del gap emerso su `/api/conversations/[id]` e sulla response reale di `live-sync`

2026-03-26 18:35 | ruolo=code-reviewer | fatto=review finale del perimetro text/live dopo la chiusura dei fix minimi residui | output=reload server-side con `stateSnapshot` confermato; fallback globali cross-conversation assenti nel perimetro verificato (`ChatContext`, `ChatShell`, `LiveModal`); `ChatInput` usa la response di `live-sync` per aggiornare subito lo snapshot client della conversazione corrente; resta solo un rischio residuo ridotto per il doppio meccanismo sync-response + refresh server-side in `LiveModal` | prossimo=attendere conferma utente; se richiesto, decidere se lasciare il refresh server-side come safety net o semplificarlo in futuro

2026-03-26 18:32 | ruolo=backend-developer | fatto=esecuzione reale del terzo fix minimo residuo sulla race post-turn live | output=creato snapshot locale `bkp/backups/2026-03-26/1834_cp17-step17-chatinput-live-sync`; `src/components/chat/ChatInput.tsx` ora usa la response di `/api/chat/live-sync` per aggiornare subito `stateSnapshot` conversation-scoped e notifica i consumer client con un evento `storage` sintetico; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, eseguire una review finale del perimetro minimo text/live appena chiuso

2026-03-26 18:30 | ruolo=backend-developer | fatto=esecuzione reale del secondo fix minimo residuo sul fallback globale del ramo live | output=creato snapshot locale `bkp/backups/2026-03-26/1831_cp16-step16-livemodal-scoped`; `src/components/chat/live/LiveModal.tsx` non legge piu ne scrive piu la chiave globale `livewell_case_state_snapshot` e usa solo bootstrap server, storage conversation-scoped e stato in memoria sessione; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, chiudere il rischio residuo di race post-turn live nel punto minimo corretto (`ChatInput.tsx`)

2026-03-26 18:28 | ruolo=backend-developer | fatto=esecuzione reale del primo fix minimo residuo sui fallback globali in `ChatShell` | output=creato snapshot locale `bkp/backups/2026-03-26/1826_cp15-step15-chatshell-scoped`; `src/components/chat/ChatShell.tsx` non legge piu la chiave globale `livewell_case_state_snapshot` e usa solo lo snapshot conversation-scoped per `conversationId`; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, rimuovere il fallback globale residuo in `src/components/chat/live/LiveModal.tsx`

2026-03-26 18:22 | ruolo=code-reviewer | fatto=review mirata del perimetro text/live dopo la chiusura dei tre blocchi minimi | output=reload server-side con `stateSnapshot` confermato; ancora presenti fallback globali cross-conversation in `ChatShell` e `LiveModal`; aggiornamento live post-turn attivo ma con race residua rispetto a `live-sync` best-effort | prossimo=attendere conferma utente; se richiesto, definire il perimetro minimo per chiudere i finding residui

2026-03-26 00:12 | ruolo=debugger | fatto=correzione locale del bug birthDate approssimata derivata da age | output=allineati i fallback age→birthDate in `inputInference.ts`, `handlers.ts` e `profile/[domain]/page.tsx` alla convenzione `YYYY-06-15`; aggiunto test smoke su `user.setAttribute age`; verdi `9/9` test mirati e `typecheck` | prossimo=attendere conferma utente per commit/push/deploy

2026-03-26 00:06 | ruolo=code-reviewer | fatto=review mirata del fix live/intake pubblicato sui 4 punti richiesti | output=nessuna regressione nuova trovata nel perimetro; punti 3 e 4 confermati con evidenze di codice+test; punti 1 e 2 confermati solo a livello di wiring codice, non verificabili end-to-end in produzione dal terminale; alias production `https://livewell.mottisi.com` raggiungibile | prossimo=eventuale smoke UI autenticato/manuale della sessione live reale

2026-03-25 23:38 | ruolo=backend-developer | fatto=implementazione e publish del piano A+B+C su intake/profile extraction/live mode | output=`agentPrompt.ts`, `inputInference.ts`, `intakeQuestions.ts`, `interviewFlow.ts`, `live-token/route.ts`, `LiveModal.tsx`, `ChatInput.tsx` allineati; test mirati verdi, `typecheck` + `build` verdi; commit `1f527be`, push `origin/main`, deploy production Vercel aliasato su `https://livewell.mottisi.com`, smoke HTTP positivo | prossimo=eventuale smoke UI manuale della sessione live reale

2026-03-18 21:08 | ruolo=llm-architect | fatto=analisi completa del runtime multi-agent, prompt team, persistence, SSE, schema Prisma e test | output=base evidenze per report Fase 1/Fase 2; creati bkp/ops e backup locale minimo | prossimo=consegna report architetturale e decisionale
2026-03-18 22:35 | ruolo=llm-architect | fatto=definizione del primo vertical slice incrementale per protocollo collaborativo reale | output=Fase 3 con `CaseState` minimo, decisione netta su `activeSpecialist`, file nuovi/legacy, test e punto di stop | prossimo=implementazione del primo slice senza toccare la UI
2026-03-18 23:15 | ruolo=llm-architect | fatto=implementazione reale del vertical slice `CaseState` con owner/speaker/consult/takeover/return baton | output=nuovo layer `src/lib/ai/case/`, persistenza Prisma, integrazione runtime/stream, `activeSpecialist` derivato, 24 test mirati verdi + typecheck ok | prossimo=eventuale step successivo su handoff permanente e capability registry
2026-03-18 23:30 | ruolo=llm-architect | fatto=refactor completo sopra il vertical slice: handoff permanente, capability contracts runtime, artifact governance e riduzione della logica residua in synthesis/consensus | output=nuovi moduli `src/lib/ai/capabilities/` e `src/lib/ai/artifacts/`, protocollo `CaseState` esteso, loader TEAM runtime-aware, 33 test mirati verdi + typecheck ok | prossimo=cleanup successivo del legacy residuo (`activeSpecialist`, `routing.ts`, ulteriori estrazioni da `synthesis.ts`)
2026-03-18 23:48 | ruolo=llm-architect | fatto=audit finale del refactor e chiusura del residuo aperto | output=`activeSpecialist` tolto dagli input backend, legacy routing confinato in `routingLegacy.ts`, `synthesisContext.ts` estratto, parser capability irrobustito, 57 test verdi + typecheck ok | prossimo=nessun passo obbligatorio aperto per questo task
2026-03-19 00:14 | ruolo=backend-developer | fatto=pubblicazione remota del refactor multi-agente completato | output=commit `30651f7`, push su `origin/main`, deploy production Vercel `https://livewell.mottisi.com`, build/typecheck/test mirati verdi | prossimo=nessun passo obbligatorio aperto; restano solo compatibilita deliberate e cleanup futuri facoltativi
2026-03-19 00:35 | ruolo=code-reviewer | fatto=review finale del refactor multi-agente pubblicato | output=3 finding reali localizzati su `activeSpecialist`, API pubblica legacy di `routing.ts` e residuo semantico in `synthesis.ts`; nessun blocco architetturale nuovo | prossimo=eventuale cleanup mirato backend-only, senza UI
2026-03-19 00:38 | ruolo=backend-developer | fatto=cleanup tecnico finale post-review del refactor multi-agente | output=re-export legacy rimosso da `routing.ts`, immediate thinking svincolato da `activeSpecialist`, nota team-mode basata sulle proposal eliminata, 22 test mirati verdi + typecheck ok | prossimo=nessun passo obbligatorio aperto nel perimetro backend-only richiesto
2026-03-19 00:42 | ruolo=backend-developer | fatto=publish remoto del cleanup backend verificato | output=commit `dcc68ae`, push su `origin/main`, deploy production Vercel `https://livewell.mottisi.com`, typecheck e 22 test mirati verdi | prossimo=nessun passo obbligatorio aperto; restano solo compatibilita deliberate e cleanup futuri facoltativi
2026-03-19 00:48 | ruolo=code-reviewer | fatto=campagna di validazione comportamentale del sistema multi-agente rifattorizzato | output=42 scenari classificati (29 PASS, 6 FAIL, 5 PARTIAL, 2 NON VERIFICABILI), 49 test verdi, harness temporanei su protocollo/artifact/capabilities, verdict: sistema collaborativo reale ma ancora solo parzialmente affidabile | prossimo=eventuali micro-correzioni mirate su capability contracts, artifact governance e stream cosmetico residuo
2026-03-19 01:01 | ruolo=backend-developer | fatto=correzione mirata dei bug B1-B5 della validazione runtime | output=trigger capability integrati nel protocollo, artifact governance ristretta e con prerequisiti minimi, synthesis piu prudente con dati mancanti, stream piu protocol-first, `typecheck` + `build` + 27 test mirati verdi | prossimo=commit, push e deploy del fix backend
2026-03-19 01:14 | ruolo=code-reviewer | fatto=review finale dei fix B1-B5 pubblicati | output=B1/B2/B3/B5 confermati corretti nel codice reale, B4 solo parzialmente corretto per una istruzione residua permissiva in `src/lib/ai/artifacts/contracts.ts`, `typecheck` + 27 test mirati rieseguiti verdi | prossimo=eventuale micro-fix mirato solo su B4, non richiesto in questo step
2026-03-19 01:17 | ruolo=backend-developer | fatto=micro-fix mirato del residuo B4 | output=allineata l'ultima istruzione di `buildProfessionalOutputInstructions()` al gating prudente di `synthesis.ts`, test di synthesis rafforzato, `typecheck` + 5/5 test verdi | prossimo=commit, push e deploy del micro-fix
2026-03-19 01:28 | ruolo=backend-developer | fatto=verifica finale post-publish del micro-fix B4 e salvataggio memoria operativa | output=`HEAD` e `origin/main` allineati su `927c823`, nessuna modifica tracked pendente, solo `.claude/` non tracciato fuori publish | prossimo=nessun passo obbligatorio aperto
2026-03-19 01:33 | ruolo=code-reviewer | fatto=campagna massiva di simulazioni comportamentali post-fix sul sistema multi-agente | output=73 scenari classificati, 47 test repository verdi, `typecheck` verde, residui veri trovati su trigger testuali troppo permissivi, owner arbitrario su input generici, detection critica incompleta e stream ancora parzialmente cosmetico | prossimo=eventuali micro-fix mirati sui residui validati
2026-03-19 08:25 | ruolo=backend-developer | fatto=micro-fix mirati sui residui della validazione massiva | output=trigger matcher token-aware, owner neutro su `general`, red flags health rafforzate, immediate thinking events soppressi su casi generici/ambigui, 25/25 test mirati verdi + `typecheck` ok | prossimo=nessun passo obbligatorio aperto nel perimetro dei quattro fix
2026-03-19 09:19 | ruolo=code-reviewer | fatto=review finale del commit `8ee6da1` sui micro-fix runtime | output=trigger matching, owner neutro, detection health critica e stream meno cosmetico confermati nel codice reale; 25/25 test mirati verdi + `typecheck` ok; nessun finding reale nel perimetro | prossimo=nessun passo obbligatorio aperto
2026-03-19 10:03 | ruolo=code-reviewer | fatto=campagna estesa di validazione comportamentale post-fix con 100 scenari | output=100 scenari classificati (59 PASS, 25 FAIL, 16 PARTIAL), 55 test repository verdi + `typecheck` ok; confermati saluti e artifact, ma triage nutrizionale implicito e handoff/consulti impliciti ancora fragili | prossimo=eventuali micro-fix mirati su triage nutrizionale implicito, ranking consult target, handoff impliciti e alcuni path di gating
2026-03-19 10:40 | ruolo=code-reviewer | fatto=campagna sistemica di validazione dell'intero team multi-agent con copertura cross-domain, memoria e allegati | output=230 scenari complessivi (149 PASS, 70 FAIL, 11 PARTIAL), 24 professionisti reali coperti, 55+16 test verdi + `typecheck` ok; saluti, monodominio esplicito, artifact, memoria e allegati reggono meglio di consulti impliciti, takeover e handoff | prossimo=eventuali micro-fix mirati solo su triage implicito, consult target ranking, takeover/return naturale e handoff impliciti
2026-03-19 12:13 | ruolo=backend-developer | fatto=micro-fix cross-domain sui residui emersi dalla campagna sistemica da 230 scenari | output=`domainDetection.ts`, `registry.ts`, `protocol.ts`, `compat.ts` e `synthesis.ts` corretti su triage implicito, consult target, takeover/handoff naturali, gating strutturato e residuo artifact ibrido; 34/34 test mirati verdi + `typecheck` + `build` verdi | prossimo=commit, push e deploy dei micro-fix applicativi
2026-03-19 14:37 | ruolo=code-reviewer | fatto=campagna comparativa post-fix sul commit `41612ca` con copertura sistemica allargata | output=325 scenari classificati (243 PASS, 36 PARTIAL, 46 FAIL), miglioramenti reali su triage implicito, consulti impliciti, takeover naturale, artifact e gating; 70/72 test repository verdi + `typecheck` ok; trovate 2 regressioni reali in `tests/api/orchestrator-domain-persistence.test.ts` | prossimo=eventuali micro-fix mirati su queue persistence/interview flow e residui ancora aperti di consulto/handoff impliciti
2026-03-19 18:15 | ruolo=backend-developer | fatto=micro-fix mirati post-validazione comparativa sui residui runtime ancora aperti | output=regressione `pendingQuestions` chiusa, handoff impliciti naturali rafforzati, consulti impliciti `health/legal/inspiration` migliorati, gating piu uniforme su `programma/protocollo/report`; 37/37 test mirati + 23/23 suite adiacenti verdi, `typecheck` e `build` ok | prossimo=commit, push, deploy e nuova validazione mirata post-fix
2026-03-19 19:40 | ruolo=code-reviewer | fatto=campagna comparativa avanzata post-fix sul commit `44df3a1` | output=369 scenari classificati (314 PASS, 13 PARTIAL, 42 FAIL), 66 test repository verdi + `typecheck` ok; queue `pendingQuestions` chiusa, gating strutturato molto migliorato, handoff e consulti impliciti migliorati ma ancora incompleti, monodominio implicito ancora area piu debole | prossimo=eventuale micro-fix mirato solo sui residui confermati oppure validazione finale conclusiva
2026-03-19 20:54 | ruolo=backend-developer | fatto=micro-fix mirati sui residui post-validazione avanzata | output=`domainDetection.ts`, `registry.ts` e `protocol.ts` corretti su monodominio implicito, consulti impliciti `legal/dermatologia/cardiologia/gastro` e same-domain handoff/takeover; 36/36 test mirati + 28/28 suite adiacenti verdi, `typecheck` + `build` ok | prossimo=commit, push, deploy e nuova validazione mirata post-fix
2026-03-19 21:22 | ruolo=code-reviewer | fatto=validazione mirata post-fix del commit `442523a` | output=36/36 test dichiarati verdi, 18/18 suite adiacenti verdi, harness runtime con team reale su 41 controlli complessivi; miglioramenti reali confermati su consulti impliciti e handoff same-domain, ma monodominio implicito e takeover naturale solo parzialmente corretti | prossimo=eventuale micro-fix mirato su owner impliciti ancora deboli e takeover troppo aggressivo
2026-03-19 22:13 | ruolo=backend-developer | fatto=micro-fix stretti post-review su owner implicito, consulto legal e same-domain takeover | output=`registry.ts` e `protocol.ts` corretti per stringere consulto family-law, rafforzare owner implicito nutrition/financial/dermatologia e lasciare takeover same-domain su phrasing morbidi; 42/42 test mirati + 28/28 suite adiacenti verdi, `typecheck` + `build` ok | prossimo=commit, push, deploy e nuova validazione mirata sui cluster corretti
2026-03-20 11:39 | ruolo=code-reviewer | fatto=validazione mirata del commit `c9b21d2` sui tre residui dichiarati corretti | output=inspection di `registry.ts` e `protocol.ts`, 42/42 test dichiarati verdi, 18/18 suite adiacenti verdi, harness runtime con team reale; owner implicito confermato, consulto legal migliorato ma ancora incompleto, same-domain takeover migliorato ma con un phrasing ancora troppo aggressivo, nessuna regressione reale | prossimo=eventuale micro-fix stretto su consulto legal generico e `parliamo ancora di questo con lui`
2026-03-20 11:59 | ruolo=backend-developer | fatto=micro-fix finale post-review su consulto legal implicito e takeover same-domain residuo | output=`registry.ts` e `protocol.ts` corretti sui due casi falliti della review (`problemi legali con la separazione`, `parliamo ancora di questo con lui`); 37/37 test mirati + 24/24 suite di guardia verdi, `typecheck` + `build` ok | prossimo=commit, push, deploy e nuova validazione mirata conclusiva
2026-03-20 12:35 | ruolo=code-reviewer | fatto=validazione finale del commit `f8093cb` sui due micro-fix finali | output=inspection di `registry.ts` e `protocol.ts`, 37/37 test dichiarati verdi, 24/24 suite di guardia verdi, harness runtime con team reale; consulto legal e takeover su `parliamo ancora di questo con lui` confermati, nessuna regressione reale, ma un phrasing forte (`vorrei che fosse lui a seguirmi da ora`) resta takeover invece di handoff | prossimo=eventuale ultimo micro-fix stretto sul solo phrasing handoff forte
2026-03-20 12:42 | ruolo=backend-developer | fatto=ultimo micro-fix stretto sul phrasing forte di handoff same-domain | output=`protocol.ts` corretto per promuovere a `handoff_pending_user` frasi di ownership stabile come `vorrei che fosse lui a seguirmi da ora`; `case-protocol.test.ts` rafforzato con positivi forti ed eseguiti `39/39` test mirati, `24/24` suite di guardia, `typecheck` e `build` verdi | prossimo=commit, push, deploy e nuova validazione mirata post-fix
2026-03-20 12:51 | ruolo=code-reviewer | fatto=validazione finale del commit `1a2101d` sul cluster same-domain handoff/takeover | output=inspection di `protocol.ts`, `39/39` test dichiarati verdi, `24/24` suite di guardia verdi, mini harness runtime con team reale sui casi forti/morbidi/return; confermati handoff forte su `vorrei che fosse lui a seguirmi da ora` e phrasing equivalente, takeover morbidi preservati, nessuna regressione reale | prossimo=eventuale validazione più ampia; nessun ulteriore micro-fix obbligatorio per questo cluster
2026-03-20 15:50 | ruolo=code-reviewer | fatto=campagna sistemica finale di validazione del sistema multi-agente sul baseline `1a2101d` | output=525 scenari distinti (462 PASS, 45 FAIL, 18 PARTIAL), 24 professionisti reali coperti, 103 test repository principali verdi + 27 extra + 1 profile DB + 2 harness temporanei; sistema ormai reale e credibile su protocollo/memory/artifacts/gating, ma ancora solo parzialmente affidabile per monodominio implicito e alcuni casi sporchi/consulti impliciti | prossimo=eventuale step di prodotto o validazione più ampia; non emerge un micro-fix immediato obbligatorio fuori dai residui impliciti
2026-03-20 16:07 | ruolo=backend-developer | fatto=micro-fix mirati sui residui impliciti cross-domain emersi dalla campagna sistemica finale | output=`domainDetection.ts`, `protocol.ts` e `registry.ts` corretti su dominio implicito, owner implicito, consulti residui e dirty cases; test aggiornati (`53/53` mirati verdi), guardie (`18/18` verdi), `typecheck` e `build` verdi | prossimo=commit, push, deploy e nuova validazione mirata post-fix
2026-03-20 16:35 | ruolo=code-reviewer | fatto=validazione mirata post-fix del commit `67db453` | output=inspection dei tre file runtime, `53/53` test dichiarati verdi, `18/18` guardie verdi, harness runtime col team reale; miglioramenti reali confermati su owner implicito e consulti residui, ma practical separation e alcuni dirty cases restano solo parzialmente credibili | prossimo=eventuale micro-fix stretto su owner practical separation e reason/target dei dirty cases più deboli
2026-03-20 17:50 | ruolo=backend-developer | fatto=correzione del fail CI reale su interview flow transcript e pulizia warning lint localizzati | output=fail classificato come test vecchio rispetto al runtime attuale (batch baseline fino a 3 domande in `interviewFlow.ts`), test aggiornato in `tests/api/interview-flow-transcript-2026-03-11.test.ts`, warning lint puliti in `handlers.ts`, `contextCache.ts`, `NutritionSection.tsx`, `ProfileNav.tsx`; verdi il test bloccante, 30 test adiacenti, targeted eslint e `typecheck` | prossimo=commit, push, deploy e verifica post-publish
2026-03-20 18:59 | ruolo=code-reviewer | fatto=checkpoint richiesto dall'utente dopo la chiusura del cluster CI | output=memoria aggiornata (`STATUS`, `WORKLOG`, journal, chatlog) e snapshot Git creato dal commit applicativo `8705b97` su branch backup dedicato; repository tracked pulito, solo `.claude/` fuori scope | prossimo=eventuale review mirata del commit `8705b97` oppure nuova validazione comportamentale
2026-03-20 22:01 | ruolo=qa-expert | fatto=validazione mirata della continuità conversazionale su casi specialistici già avviati | output=30 scenari multi-turno classificati, 75/75 test guardia verdi, harness temporaneo su team reale; protocollo tecnico confermato ma continuità conversazionale solo parziale, con intake fuori timing e perdita di focus reale su burnout/sonno/finanza/separazione pratica | prossimo=eventuale micro-fix stretto sui moduli del cluster conversazionale, senza refactor generale
2026-03-21 09:16 | ruolo=backend-developer | fatto=micro-fix stretti sul cluster conversazionale dei follow-up specialistici | output=`interviewFlow.ts`, `domainDetection.ts`, `protocol.ts`, `registry.ts`, `synthesis.ts` corretti su intake fuori timing, drift burnout/sonno, return baton prematuro legal/financial e resume con memoria; `53/53` test conversazionali mirati verdi + `37/37` guardie verdi + `typecheck`/`build` ok | prossimo=commit, push, deploy e nuova validazione mirata post-fix
2026-03-21 09:36 | ruolo=code-reviewer | fatto=validazione mirata post-fix del commit `1b64da1` sul cluster conversazionale | output=inspection di `interviewFlow.ts`, `domainDetection.ts`, `protocol.ts`, `registry.ts`, `synthesis.ts`; `90/90` test repository verdi; mini harness su 12 scenari con team reale: miglioramenti veri confermati su intake fuori timing, consulti legal/financial attivi e resume reflusso, ma findings reali residui su separazione pratica e resume sonno | prossimo=eventuale micro-fix strettissimo su quei due residui
2026-03-21 09:45 | ruolo=backend-developer | fatto=micro-fix strettissimi sui due finding conversazionali residui | output=`domainDetection.ts`, `protocol.ts`, `registry.ts` corretti su practical separation e sleep resume; test aggiornati in `domain-detection-critical`, `case-protocol`, `runtime-trigger-guards`; `94/94` test richiesti verdi + `typecheck` + `build` verdi + harness runtime col team reale positivo | prossimo=commit, push, deploy e nuova validazione mirata post-fix
2026-03-23 19:19 | ruolo=code-reviewer | fatto=ricostruzione modifiche locali non committate lasciate da Claude | output=`HEAD`=`origin/main` su `ba7170a`; 12 file tracked modificati + 3 file progetto untracked + artefatti `.claude/`; feature principale individuata: triage multi-dominio con quick replies SSE/UI e raffinamento euristiche health/fisio; nessun commit/push/deploy eseguito per override utente | prossimo=attendere conferma utente prima di validazione, commit, push o deploy
2026-03-23 19:21 | ruolo=backend-developer | fatto=separazione precisa tra file del feature diff e residui da escludere | output=classificati come sicuramente da committare i 12 file tracked del flusso quick replies/triage multi-dominio + `multiDomainTriage.ts`; classificati come da escludere `routingLegacy.ts`, `tests/orchestrator-routing.test.ts`, `.claude/launch.json`, `.claude/worktrees/*` e i file memoria `bkp/ops/*`; evidenza forte: `routeMessage` non esiste nel codice attuale e `tests/api/orchestrator-routing-phase1.test.ts` dichiara il path legacy come morto/sostituito | prossimo=attendere conferma utente; se richiesto, preparare staging list esatta senza ancora committare
2026-03-23 19:23 | ruolo=backend-developer | fatto=preparazione staging list finale senza eseguire `git add` | output=definito il set finale di 13 file da includere nel futuro commit e il set esplicito di file/cartelle da lasciare fuori (`routingLegacy.ts`, test legacy, `.claude/**`, `bkp/ops/**`); nessuna azione Git eseguita oltre alla lettura dello stato | prossimo=attendere conferma utente prima di stage mirato o publish
2026-03-23 19:40 | ruolo=backend-developer | fatto=raccolta data/ora di ultima modifica dei 13 file approvati | output=timestamp filesystem locale rilevati con `stat`; gruppo UI/API modificato tra `18:03` e `18:06`, gruppo runtime/domain tra `18:43` e `18:44` del `2026-03-23`; nessuna modifica applicata e nessuna azione Git eseguita | prossimo=attendere conferma utente; se richiesto, mostrare anche autore/ultima commit per ciascun file
2026-03-23 19:44 | ruolo=backend-developer | fatto=raccolta ultimo commit/autore/data per i 13 file approvati | output=per i 12 file tracked recuperato l'ultimo commit file-by-file con `git log -1`; `multiDomainTriage.ts` confermato come file nuovo non tracciato senza storico Git; nessuna azione di stage/commit/push/deploy eseguita | prossimo=attendere conferma utente; se richiesto, confrontare storico Git e mtime locale per misurare quali file sono stati effettivamente modificati dopo il loro ultimo commit
2026-03-23 21:46 | ruolo=code-reviewer | fatto=analisi del bug da screenshot su passaggio specialisti | output=confermata esistenza del protocollo di consult/handoff in `src/lib/ai/case/protocol.ts`, ma trovato bug reale: durante `consult_active_takeover` una richiesta esplicita verso un terzo specialista (`requestedAgentId != consultTarget`) ricade nel branch `return_baton` invece di aprire un nuovo consulto; il thinking del fisiatra che dice di non poter passare il caso non corrisponde a una regola testuale nei capability file, ma e un effetto secondario del buco di state machine e dei confini di ruolo | prossimo=attendere conferma utente; se richiesto, applicare fix stretto al branch `consult_active_takeover`
2026-03-23 21:58 | ruolo=backend-developer | fatto=fix completo export conversazioni con feedback, con publish remoto | output=allineati `assistantId` stream/DB in `chatPersistence.ts`, `chat/send/route.ts` e `ChatContext.tsx`; export feedback irrobustito in `src/app/api/conversations/[id]/export/route.ts` con fallback legacy; test aggiunti/aggiornati (`442/442` verdi), `typecheck` e `build` verdi; commit `8236b8a`, push su `origin/main`, deploy production Vercel aliasato su `https://livewell.mottisi.com` e smoke HTTP positiva | prossimo=attendere feedback utente; se richiesto, eseguire smoke UI sul download `.txt` con feedback reali in produzione
2026-03-23 22:17 | ruolo=backend-developer | fatto=persistenza thinking traces in chat/export con publish remoto | output=aggiunto helper `src/lib/chat/thinkingPersistence.ts`; il thinking degli eventi `agent.thinking` viene ora serializzato nel messaggio assistant, ricaricato in `GET /api/conversations/[id]`, escluso da preview/context pack/live-token e stampato nell'export `.txt`; `445/445` test verdi, `typecheck` e `build` verdi; commit `e370ea6`, push su `origin/main`, deploy production Vercel aliasato su `https://livewell.mottisi.com` e smoke HTTP positiva | prossimo=attendere feedback utente; se richiesto, fare smoke manuale del download chat+feedback+thinking da UI
2026-03-23 22:35 | ruolo=backend-developer | fatto=correzione export del reasoning completo delle proposal con publish remoto | output=`chat/send/route.ts` aggiornato per persistere prioritarimente il reasoning completo di `round1/round2Proposals` invece dei soli status `agent.thinking`; export `.txt` aggiornato a stampare multilinea completa e UI `MessageBubble` resa `pre-wrap`; `446/446` test verdi, `typecheck` e `build` verdi; commit `e7d8c9b`, push su `origin/main`, deploy production Vercel aliasato su `https://livewell.mottisi.com` e smoke HTTP positiva | prossimo=attendere nuovo export utente da produzione per conferma finale del formato completo
2026-03-26 00:19 | ruolo=backend-developer | fatto=correzione stretta della derivazione errata `age -> birthDate` | output=`agentPrompt.ts`, `inputInference.ts`, `handlers.ts` e `profile/[domain]/page.tsx` allineati per non inventare piu mese/giorno; l'eta resta `personal.age` se l'utente non fornisce la data completa; test mirati `9/9` verdi, `typecheck` verde, `build` verde | prossimo=commit, push, deploy production e smoke rapido post-publish
2026-03-26 00:21 | ruolo=backend-developer | fatto=publish remoto del fix `age -> birthDate` senza supposizioni | output=commit `e0ea072` (`fix: stop guessing birth dates from age`), push completato su `origin/main`, deploy production Vercel avviato e alias `https://livewell.mottisi.com` verificato via `curl` con risposta HTML valida della login page | prossimo=nessun passo applicativo obbligatorio aperto; solo eventuale smoke UI autenticato
2026-03-26 12:15 | ruolo=code-reviewer | fatto=verifica richiesta utente su mutabilita temporale dei dati come `age` | output=finding confermato: la mutabilita automatica esiste solo quando e presente `birthDate` reale; se viene salvato solo `personal.age`, UI e synthesis lo mostrano come dato statico e non c'e un modello generale `measurement_date`/derivazione temporale implementato | prossimo=se richiesto, fix strutturale del modello dati mutevoli con commit/push/deploy
2026-03-26 12:55 | ruolo=backend-developer | fatto=refactor strutturale del dynamic DB e delle integrazioni multi-agente | output=aggiunta semantica canonica per dati osservati vs derivabili (`src/lib/dynamicDb/semantics.ts`), export `/api/profile/dynamic-db` esteso con `derived` e `documents`, upload file resi consultabili/scaricabili con riferimento nel dynamic DB, artifact salvati anche come record dinamici con notes, contesto multi-agente arricchito con note documenti, UI cartella aggiornata con età derivata solo da `birthDate` e sezione documenti/artifact; verdi `12/12` test mirati, `typecheck`, `build` | prossimo=commit, push, deploy production e smoke rapido post-publish
2026-03-26 13:00 | ruolo=backend-developer | fatto=publish remoto del refactor dynamic DB semantico | output=commit `23eb158` (`feat: unify dynamic db semantics and documents`), push completato su `origin/main`, deploy Vercel production completato con URL `https://livewell-78ot36wsk-iamotts-projects.vercel.app` e alias `https://livewell.mottisi.com`; smoke HTTP positivo sull'alias | prossimo=attendere feedback utente o, se richiesto, eseguire smoke UI autenticato su documenti/artifact
2026-03-26 13:47 | ruolo=code-reviewer | fatto=analisi approfondita dei problemi su thinking espandibile, selezione specialista e colorazione UI chat | output=confermate cause multiple: `ricomposizione corporea` non aggancia bene il dominio iniziale e puo lasciare owner `orchestratore`; `chat/send/route.ts` persiste solo parte dei thinking events; `specialistName`/`activeSpecialistId` dipendono da `consensus.activeSpecialist` mentre i proposal possono mostrare `Dietista` anche senza modalita specialista; `loadConversation()` azzera `activeDomain` e non ricostruisce `activeSpecialistId` | prossimo=attendere conferma utente; se richiesto, trasformare i finding in piano operativo o fix mirato
2026-03-26 14:44 | ruolo=code-reviewer | fatto=verifica architetturale del cuore multi-agente rispetto al modello desiderato di panel multi-dominio vivo e progressivo | output=verdetto negativo: il sistema oggi e collaborativo per turno ma non nel modello richiesto; mantiene un solo `ownerAgentId` e un solo `activeSpeakerAgentId`, seleziona un set globale di agenti con score/keyword/domain rules, usa quick-reply triage per i casi multi-dominio e promuove al massimo un `activeSpecialist`; il dynamic DB viene letto a ogni turno e scritto dai tool, ma non esiste ancora un comitato persistente di un agente per dominio attivo in parallelo | prossimo=attendere conferma utente; se richiesto, derivare piano di riallineamento architetturale senza modifiche o passare all'implementazione
2026-03-26 15:32 | ruolo=backend-developer | fatto=produzione del piano operativo dettagliato per riallineare il runtime multi-agente al modello multi-dominio persistente richiesto | output=confermato che scoring/piramide restano ma vengono subordinati al contesto; definita sequenza in fasi su `CaseState`, routing contestuale per dominio, preselezione e selezione per dominio, panel di specialisti attivi, consenso composito, synthesis multi-voce controllata, SSE/UI state, live-sync e Dynamic DB; nessuna modifica codice eseguita | prossimo=attendere conferma utente; se richiesto, aprire implementazione fase 1 partendo dal nuovo state model e dai contratti types/transport
2026-03-26 15:38 | ruolo=backend-developer | fatto=estensione del piano operativo al ramo live audio/video Gemini Live | output=verificato che il ramo live oggi dipende ancora da `activeSpeakerAgentId/ownerAgentId` singolari in `live-token/route.ts`, nasconde la modalita specialista durante la sessione in `ChatShell.tsx` e in `live-sync/route.ts` attribuisce i tool ad un solo `capabilityAgentId`; aggiornato il piano per imporre un contratto condiviso text/live con continuita di panel, UI e Dynamic DB | prossimo=attendere conferma utente; se richiesto, trasformare il piano in sequenza esecutiva per la fase 1 shared text/live
2026-03-26 15:47 | ruolo=backend-developer | fatto=produzione del piano tecnico-esecutivo dettagliato della sola Fase 1 shared text/live | output=definiti contratto canonico di stato, shared contracts text/live, ordine reale file-by-file, compat layer temporaneo, campagna test minima, checkpoint/log/resume plan e criteri di completamento senza modifiche al codice | prossimo=attendere conferma utente; se richiesto, iniziare l'implementazione dalla Fase 1 seguendo il piano e i checkpoint nominati
2026-03-26 15:58 | ruolo=backend-developer | fatto=esecuzione reale di `CP-0` e `STEP-1` della Fase 1 shared text/live | output=creato snapshot locale `bkp/backups/2026-03-26/1558_cp0-step1-types` dei file target; introdotti in `src/lib/ai/types.ts` i nuovi tipi canonici `DomainPanel`, `ConversationFocus`, `CoordinationState`, `SpeakerPolicy`, `CanonicalCaseStateSnapshot`; estesi in modo additivo `AgentInput.caseStateSnapshot?` e `ConsensusResult.stateSnapshot?`; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, procedere con `CP-2` + `STEP-2` su `src/lib/ai/case/state.ts`
2026-03-26 16:02 | ruolo=backend-developer | fatto=esecuzione reale di `CP-2` e `STEP-2` della Fase 1 shared text/live | output=creato snapshot locale `bkp/backups/2026-03-26/1602_cp2-step2-state`; `src/lib/ai/case/state.ts` esteso con il nuovo shape canonico opzionale e con un normalizer duale che accetta payload legacy o canonical-first, sintetizzando fallback legacy minimi quando necessario; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, procedere con `CP-3` + `STEP-3` su `src/lib/ai/case/compat.ts`
2026-03-26 16:05 | ruolo=backend-developer | fatto=esecuzione reale di `CP-3` e `STEP-3` della Fase 1 shared text/live | output=creato snapshot locale `bkp/backups/2026-03-26/1605_cp3-step3-compat`; `src/lib/ai/case/compat.ts` esteso con i mapper centrali `toCanonicalCaseStateSnapshot()` e `applyCanonicalSnapshotToLegacyCaseState()`; `deriveActiveSpecialistFromCaseState()` allineato al panel lead canonico; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, procedere con `CP-4` + `STEP-4` su `src/lib/ai/case/persistence.ts`
2026-03-26 16:08 | ruolo=backend-developer | fatto=esecuzione reale di `CP-4` e `STEP-4` della Fase 1 shared text/live | output=creato snapshot locale `bkp/backups/2026-03-26/1608_cp4-step4-persistence`; `src/lib/ai/case/persistence.ts` esteso con `fromStoredCaseState()` per dual-read e `toStoredCaseStateWithCanonicalSnapshot()` per il future dual-write, mantenendo `toStoredCaseState()` legacy-safe per i caller Prisma attuali; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, intervenire sui consumer del layer persistence
2026-03-26 16:16 | ruolo=backend-developer | fatto=analisi del prossimo perimetro di adozione dei nuovi helper di persistence | output=identificato `src/app/api/chat/send/chatPersistence.ts` come primo consumer corretto da aggiornare, seguito da `src/app/api/chat/send/route.ts`, `src/app/api/chat/live-sync/route.ts` e solo dopo `src/contexts/ChatContext.tsx`; esclusi per ora `live-token` e UI bootstrap per ridurre il rischio di divergenza text/live | prossimo=attendere conferma utente; se richiesto, implementare l'adozione in `chatPersistence.ts`
2026-03-26 16:19 | ruolo=backend-developer | fatto=esecuzione reale del primo consumer corretto per il nuovo layer di persistence | output=creato snapshot locale `bkp/backups/2026-03-26/1619_cp5-step5-chatpersistence`; `src/app/api/chat/send/chatPersistence.ts` ora usa `fromStoredCaseState()` per il dual-read e prepara il dual-write con `toStoredCaseStateWithCanonicalSnapshot()` senza tentare scritture non supportate dallo schema Prisma; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, adottare il nuovo snapshot in `src/app/api/chat/send/route.ts`
2026-03-26 16:22 | ruolo=backend-developer | fatto=esecuzione reale del transport text per il nuovo snapshot canonico | output=creato snapshot locale `bkp/backups/2026-03-26/1622_cp6-step6-send-route`; `src/app/api/chat/send/route.ts` ora allega `stateSnapshot` al payload SSE `ui.state` in modo additivo e backward-compatible, con fallback da `consensus.caseState` o `storedCaseState`; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, allineare il ramo `src/app/api/chat/live-sync/route.ts`
2026-03-26 16:39 | ruolo=backend-developer | fatto=esecuzione reale dell'allineamento live-sync al nuovo snapshot canonico | output=creato snapshot locale `bkp/backups/2026-03-26/1639_cp7-step7-live-sync`; `src/app/api/chat/live-sync/route.ts` ora passa `caseStateSnapshot` all'orchestrator, usa il lead panel canonico come fallback per i tool, persiste `consensus.stateSnapshot` anche senza `consensus.caseState` tramite compat layer e restituisce `stateSnapshot` nella response JSON; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, adottare il nuovo snapshot in `src/contexts/ChatContext.tsx`
2026-03-26 16:48 | ruolo=backend-developer | fatto=esecuzione reale del consumer client per il nuovo snapshot canonico | output=creato snapshot locale `bkp/backups/2026-03-26/1648_cp8-step8-chatcontext`; `src/contexts/ChatContext.tsx` ora legge e conserva `stateSnapshot` dagli eventi SSE `ui.state` e, se presente, dal payload di `loadConversation()`, mantenendolo anche in `localStorage`; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, estendere l'uso del nuovo snapshot ai consumer visuali o al bootstrap live
2026-03-26 16:52 | ruolo=backend-developer | fatto=esecuzione reale del primo consumer visuale per il nuovo snapshot canonico | output=creato snapshot locale `bkp/backups/2026-03-26/1652_cp9-step9-chatshell`; `src/components/chat/ChatShell.tsx` usa `stateSnapshot` da `localStorage` come fallback visuale per dominio lead e specialista lead, mantenendo retrocompatibilita completa con `activeDomain` e `activeSpecialist*`; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, allineare `src/app/api/live-token/route.ts`
2026-03-26 17:02 | ruolo=backend-developer | fatto=esecuzione reale del bootstrap live per il nuovo snapshot canonico | output=creato snapshot locale `bkp/backups/2026-03-26/1702_cp10-step10-live-token`; `src/app/api/live-token/route.ts` ora deriva un `stateSnapshot` canonico dal `CaseState` legacy, usa il lead panel come fallback prioritario per lo specialista live, arricchisce il `systemInstruction` con un riepilogo panel-aware e restituisce `stateSnapshot` nel payload JSON in modo additivo; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, adottare il nuovo snapshot in `src/components/chat/live/LiveModal.tsx`
2026-03-26 17:21 | ruolo=backend-developer | fatto=esecuzione reale del consumer live client per il nuovo snapshot canonico | output=creato snapshot locale `bkp/backups/2026-03-26/1721_cp11-step11-livemodal`; `src/components/chat/live/LiveModal.tsx` legge `stateSnapshot` dal bootstrap di `/api/live-token`, lo conserva in memoria/localStorage e lo usa in modo additivo per rinforzare il contesto shared del system instruction live; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, valutare eventuali adapter residui nel perimetro chat/live
2026-03-26 17:40 | ruolo=backend-developer | fatto=esecuzione reale del primo blocco minimo sul reload server-side | output=creato snapshot locale `bkp/backups/2026-03-26/1740_cp12-step12-conversation-route`; `src/app/api/conversations/[id]/route.ts` ora ricostruisce `stateSnapshot` dal `CaseState` legacy corrente con gli helper canonici e lo restituisce nel payload JSON del reload conversazione in modo additivo; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, chiudere i fallback globali cross-conversation in `src/contexts/ChatContext.tsx`
2026-03-26 17:42 | ruolo=backend-developer | fatto=esecuzione reale del secondo blocco minimo sui fallback client cross-conversation | output=creato snapshot locale `bkp/backups/2026-03-26/1742_cp13-step13-chatcontext-scoped`; `src/contexts/ChatContext.tsx` ora usa chiavi `localStorage` scoped per `conversationId` per `stateSnapshot` e stato specialistico, azzera lo stato specialistico all'ingresso di `loadConversation()` e salva gli aggiornamenti SSE nel namespace della conversazione corretta; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, chiudere l'allineamento del post-turn live al client
2026-03-26 17:46 | ruolo=backend-developer | fatto=esecuzione reale del terzo blocco minimo sul post-turn live al client | output=creato snapshot locale `bkp/backups/2026-03-26/1746_cp14-step14-live-postturn`; `src/components/chat/live/LiveModal.tsx` salva subito `stateSnapshot` del bootstrap e, dopo ogni turno assistant, esegue un refresh server-side della conversazione corrente per aggiornare immediatamente il nuovo snapshot lato client senza aspettare solo il reload finale; `npm run typecheck` verde | prossimo=attendere conferma utente; se richiesto, fare review mirata del perimetro text/live aggiornato
2026-03-26 23:55 | ruolo=backend-developer | fatto=chiusura del blocco architetturale post-Fase-1 verso runtime canonico shared text/live | output=`chat/send` emette compat fields derivati dal lead panel canonico, `ChatContext` e `ChatShell` leggono ora `stateSnapshot` come fonte primaria, `orchestrator` e `fastPaths` fanno vincere il canonico nei conflitti con il legacy; verdi `npm run typecheck`, `npm run build` e 108 test mirati su orchestration/text/live/security | prossimo=commit, push, deploy e verifica post-deploy finale
2026-03-27 00:32 | ruolo=backend-developer | fatto=cleanup finale non bloccante e review critica post-cleanup | output=rimossa la dead code `src/lib/ai/orchestrator/routingLegacy.ts`, eliminato l'invio client inutile di `activeSpecialistId`, tipizzato `ui.state.stateSnapshot` in `chatStream.ts` e reso coerente l'emissione SSE con `toSse()`; verdi `npm run typecheck`, `npm run build` e `npm run test -- tests/api/chat-send-persistence.test.ts tests/api/chat-send-security.test.ts tests/api/case-persistence.test.ts`; review critica completata senza finding blocking/high | prossimo=commit, push, deploy e chiusura finale con sole raccomandazioni residue non bloccanti
2026-03-27 08:16 | ruolo=backend-developer | fatto=publish finale della cleanup track con verifica production | output=commit `f90fa6c` pushato su `origin/main`; deploy Vercel production completato su `https://livewell-nimvn8cgj-iamotts-projects.vercel.app` con alias `https://livewell.mottisi.com`; verifica remota HTTP positiva con redirect auth atteso su `/login` | prossimo=nessun altro step obbligatorio aperto; restano solo raccomandazioni non bloccanti
2026-03-27 10:35 | ruolo=backend-developer | fatto=chiusura track infra/security con triage audit, fix Next/config e review finale | output=triagiate advisory npm con reachability reale; aggiornati `next`/`eslint-config-next` a `16.2.1`, `eslint` a `9.39.4`, migrato `middleware` -> `proxy`, impostato `turbopack.root`, aggiunti overrides mirati `gaxios/google-auth-library/flatted/yaml`; verdi `npm run typecheck`, `npm run build`, `npm run test -- tests/api/chat-send-persistence.test.ts tests/api/live-sync-stateSnapshot.test.ts tests/api/live-token-security.test.ts tests/api/case-persistence.test.ts`, `npm audit` ridotto a `10` vulnerabilita` (`9 moderate`, `1 high`) solo infra/dev-toolchain residue; deploy production `livewell-nkthbc9i1-iamotts-projects.vercel.app` verificato su alias `livewell.mottisi.com` | prossimo=nessun altro step obbligatorio aperto; restano solo raccomandazioni future non bloccanti
2026-03-27 10:55 | ruolo=backend-developer | fatto=major toolchain hygiene con `lint-staged@16` e `vitest@4`, piu` stabilizzazione test compatibili | output=creati backup `bkp/backups/2026-03-27/1040_cp37-toolchain-major` e `bkp/backups/2026-03-27/1052_cp38-vitest4-test-fix`; audit ridotto a `9 moderate, 0 high`; `tests/api/live-token-security.test.ts` e `tests/api/live-token-fallback-observability.test.ts` adattati a `vitest@4`; verdi `npm run lint`, `npm run typecheck`, `npm run build`, 33 test mirati shared-runtime/security; nessuna regressione applicativa osservata | prossimo=commit mirato, push su `origin/main`, deploy production e review finale della track
2026-03-27 11:24 | ruolo=backend-developer | fatto=publish finale della track toolchain/dependency hygiene | output=commit `a3c925a` pushato su `origin/main`; deploy Vercel production completato su `https://livewell-bx4pz8th3-iamotts-projects.vercel.app` con alias `https://livewell.mottisi.com`; verifica HTTP finale positiva con redirect auth `307 /login` | prossimo=nessun altro step operativo obbligatorio aperto; restano solo raccomandazioni future non bloccanti
2026-03-27 12:08 | ruolo=code-reviewer | fatto=review totale avversariale dell'intero progetto post-closeout | output=verificati lint/typecheck/build e 27 test mirati shared-runtime; confermati finding reali: path caldo ancora ibrido con adapter legacy, mismatch text/live nella semantica tool del route text, validazione troppo permissiva del canonical snapshot, live bootstrap coperto soprattutto da test mock-heavy, warning lint/framework ancora vivi; verdetto finale=`ISSUES FOUND` | prossimo=aprire track correttiva sui finding CP-40 senza riaprire indiscriminatamente l'architettura shared text/live
2026-03-27 12:53 | ruolo=backend-developer | fatto=chiusura locale delle track correttive obbligatorie post-review avversariale | output=aggiunto `src/lib/tools/toolExecutionRouting.ts` per la selezione shared per-call dell'agent tool; `chat/send/route.ts` e `live-sync/route.ts` ora convergono sullo stesso resolver panel-aware e non ricostruiscono `CaseState` nel path caldo quando esiste `stateSnapshot`; `persistence.ts` rifiuta snapshot malformed senza fallback legacy silenzioso; `ChatShell.tsx` non descrive piu` il live come single-agent; verdi `npm run typecheck`, `npm run lint`, `npm run build` e 110 test mirati | prossimo=commit, push, deploy production e review finale dei residui non bloccanti
2026-03-27 12:59 | ruolo=backend-developer | fatto=publish finale delle track correttive obbligatorie post-review | output=commit applicativo `0f050b7` pushato su `origin/main`; deploy Vercel production completato su `https://livewell-irtjcirg2-iamotts-projects.vercel.app` con alias `https://livewell.mottisi.com`; verifica HTTP finale positiva con redirect auth `307 /login`; nessuna regressione osservata su build/typecheck/lint e 110 test mirati | prossimo=nessun altro step obbligatorio aperto; restano solo raccomandazioni future non bloccanti
2026-03-27 15:26 | ruolo=backend-developer | fatto=riconciliazione storica reale dei blocchi A/B con publish remoto e riesame finale dei blocchi C/D | output=corretta la semantica Dynamic DB di `birthDate` in `src/lib/dynamicDb/semantics.ts`; `src/app/api/profile/dynamic-db/route.ts` ora deriva `currentAge` anche dagli attributi personali storicizzati; `src/lib/tools/handlers.ts` impone note agentiche non vuote nei principali write-path del Dynamic DB; aggiunti test in `tests/api/profile-dynamic-db-route.test.ts`, `tests/api/cartella-clinica.test.ts`, `tests/api/dynamic-db-agent-notes.test.ts`; verdi `npm run typecheck`, `npm run lint`, `npm run build`, suite mirate Dynamic DB/orchestration e deploy production `https://livewell-kiuc86a3j-iamotts-projects.vercel.app` verificato su alias | prossimo=formalizzare il verdetto finale della riconciliazione: A done, B done, C done, D partial per routing ancora keyword-heavy
2026-03-27 16:16 | ruolo=backend-developer | fatto=chiusura locale della track obbligatoria sul routing multi-dominio context-first / LLM-first | output=aggiunto `src/lib/ai/orchestrator/contextualRouting.ts`; `orchestrator.ts` usa ora LLM routing + snapshot/history prima delle euristiche; `agentSelection.ts` fa vincere i preferredAgentIds ordinati del panel e riduce gli hint statici a booster; `case/protocol.ts` preserva lo speaker/panel corrente nei follow-up contestuali invece di aprire takeover guidati dal legacy; chiuso anche il warning lint in `src/components/profile/UserAvatar.tsx`; verdi `npm run typecheck`, `npm run lint`, `npm run build` e 216 test mirati su routing/orchestrator/protocol/shared text-live | prossimo=commit, push, deploy production e review finale totale per verificare che non restino track obbligatorie
2026-03-27 16:22 | ruolo=backend-developer | fatto=publish remoto della chiusura context-first con verifica production | output=commit `ca4c313` pushato su `origin/main`; deploy Vercel production completato su `https://livewell-ohwqdv69m-iamotts-projects.vercel.app` con alias `https://livewell.mottisi.com`; verifica HTTP finale positiva con redirect auth `307` su `/login` | prossimo=chiudere con review finale totale e verdetto unico sui residui rimasti
2026-03-27 16:49 | ruolo=backend-developer | fatto=cleanup residuo locale su legacy interno confinato e hardening dei test live | output=aggiunto writer snapshot-first in `src/lib/ai/case/persistence.ts`; `chatPersistence.ts`, `chat/send/route.ts` e `live-sync/route.ts` persistono ora il runtime canonico senza ricostruire `CaseState` quando lo snapshot esiste; rafforzati `tests/api/live-token-security.test.ts` e `tests/api/live-token-fallback-observability.test.ts` sui path osservabili del bootstrap live; verdi `npm run test -- tests/api/case-persistence.test.ts tests/api/chat-send-persistence.test.ts tests/api/live-sync-stateSnapshot.test.ts tests/api/live-token-security.test.ts tests/api/live-token-fallback-observability.test.ts`, `npm run typecheck`, `npm run lint`, `npm run build` | prossimo=commit, push, deploy production e review finale conclusiva dei residual recommendations
2026-03-27 16:57 | ruolo=backend-developer | fatto=publish finale del cleanup residuo con verifica production | output=commit `ba3b33d` pushato su `origin/main`; deploy Vercel production completato su `https://livewell-2niucibjl-iamotts-projects.vercel.app` con alias `https://livewell.mottisi.com`; verifica HTTP positiva con redirect auth `307` su `/login`; nessuna regressione osservata nel perimetro residuo dopo test, lint e build finali | prossimo=chiudere con verdict finale `DONE WITH RESIDUAL RECOMMENDATIONS` se la review residua non mostra nuovi finding blocking/high
2026-03-27 20:43 | ruolo=backend-developer | fatto=fix pack prioritario post-feedback reale implementato e validato localmente | output=corretto ordering transcript live serializzando le write client e persistendo solo i messaggi confermati dal route; filtrati i payload/tool interni dai contenuti assistant in transcript/load/export/list; riallineati banner specialista, label speaker e metadata assistant tra `chat/send`, `ChatContext`, `ChatInput` e `ChatShell`; aggiunti test mirati su transcript route, ordering live client, banner specialista e sanitizzazione export/load; verdi `npm run typecheck`, `npm run lint`, `npm run build` e suite mirate del fix pack | prossimo=publish remoto del fix pack e verifica alias production
2026-03-27 20:49 | ruolo=backend-developer | fatto=publish remoto del fix pack prioritario transcript/output/speaker con verifica production | output=commit `f282175` (`fix: stabilize live transcript and specialist visibility`) pushato su `origin/main`; deploy Vercel production completato su `https://livewell-2bqy39zfv-iamotts-projects.vercel.app` con alias `https://livewell.mottisi.com`; verifica HTTP finale positiva con redirect auth `307` su `/login`; nessuna regressione osservata su build, lint, typecheck e test mirati | prossimo=attendere nuovo test reale utente in production per confermare il comportamento percepito dopo il fix pack
2026-03-27 21:40 | ruolo=qa-expert | fatto=verifica production del fix pack transcript/output/speaker con utente smoke reale | output=registrato e autenticato utente smoke su production; `chat/transcript` conferma ordering corretto user->assistant in conversazione `cmn9d30ky0002rv12rhobln6q`; `Payload:` non compare in load/export ma un assistant transcript misto payload+testo visibile viene scartato interamente (`savedMessages: []`), quindi filtering = PARTIAL; `chat/send` su produzione mostra `ui.state.specialistName=Fisioterapista` e messaggio assistant persistito con `specialistName=Fisioterapista` nella conversazione `cmn9d6vqt0008rv12tyzlfjgh` | prossimo=aprire fix stretto sul sanitizer/transcript route per preservare il testo assistant visibile nei messaggi misti e poi ripetere la verifica production
2026-03-27 20:54 | ruolo=backend-developer | fatto=chiusura del follow-up stretto sul filtering transcript assistant misto payload+testo visibile | output=`src/lib/chat/userVisibleContent.ts` ora rimuove sia payload JSON sia sequenze legacy `key:"value"` all'inizio della directive interna, preservando il testo assistant residuo sulla stessa riga; aggiunti guardrail mirati in `tests/api/chat-transcript-route.test.ts` e `tests/api/conversation-thinking-export.test.ts`; verdi `npm run test -- tests/api/chat-transcript-route.test.ts tests/api/conversation-thinking-export.test.ts`, `npm run typecheck`, `npm run lint`, `npm run build`; commit `fcf51b9`, push su `origin/main`, deploy production `https://livewell-pl7axw7ty-iamotts-projects.vercel.app`; verifica production ripetuta con utente smoke reale: transcript route salva `Ti aiuto a impostare il percorso.` senza `Payload:` nella conversazione `cmn9dpqha0001lrqwlmmsieis` e nell'export | prossimo=nessun altro fix obbligatorio aperto nel perimetro transcript/output del fix pack
2026-03-27 21:56 | ruolo=qa-expert | fatto=rerun production del fix pack transcript/output/speaker dopo il follow-up filtering | output=sessione smoke production riusata con successo; `chat/transcript` PASS su ordering e filtering nella conversazione `cmn9e82do0001oc3sfh4i5q0o` con assistant misto `Payload + testo visibile` salvato come `Ti aiuto a impostare il percorso.`; export senza `Payload:`; `chat/send` PASS su coerenza speaker/specialista nella conversazione `cmn9e8ho40005oc3svaq2ilaw` con `ui.state.specialistName=Fisioterapista` e messaggio assistant persistito `specialistName=Fisioterapista` | prossimo=nessun follow-up obbligatorio aperto; attendere solo nuovi test reali utente
2026-03-27 22:11 | ruolo=backend-developer | fatto=closeout decisionale del fix pack transcript/output/speaker e scelta della prossima track separata a massimo ROI | output=nessun finding reale aperto nel perimetro del fix pack; residual recommendations confermate fuori scope; priorita` consigliata per una nuova track separata = integrazione live/browser meno mock-heavy, perche` ha il miglior rapporto rischio/beneficio rispetto a cleanup legacy interno e hygiene toolchain/Prisma | prossimo=attendere richiesta esplicita utente prima di aprire una nuova macro-track separata
2026-03-27 22:40 | ruolo=backend-developer | fatto=riattivazione visuale dei domini nella chat dopo regressione post-multi-dominio | output=`ChatShell` ora usa il dominio canonico corrente come priorita` visuale, `ChatInput` illumina piu` domini attivi e switcha quando cambia il focus, il banner alto `modalità specialista attiva` e` rimosso, `MessageBubble` colora le bolle assistant col dominio attivo di fallback; aggiunti 3 guardrail UI/domain e validate verdi `npm run test -- tests/api/chat-shell-domain-visuals.test.ts tests/api/chat-input-domain-highlights.test.ts tests/api/message-bubble-domain-color.test.ts`, `npm run typecheck`, `npm run lint`, `npm run build` | prossimo=commit, push, deploy e verifica rapida dell'alias production
2026-03-27 22:45 | ruolo=backend-developer | fatto=publish remoto del fix UI domini/chat con verifica alias | output=commit applicativo `ded544c` pushato su `origin/main`; deploy Vercel production completato su `https://livewell-klpef43xp-iamotts-projects.vercel.app` con alias `https://livewell.mottisi.com`; verifica HTTP finale positiva con redirect auth `307` verso `/login` | prossimo=nessun fix obbligatorio aperto nel perimetro; attendere solo eventuale verifica manuale browser-side o nuovi feedback reali
2026-03-27 22:59 | ruolo=qa-expert | fatto=verifica production del fix UI domini/chat sui 5 controlli richiesti | output=FAIL su domini attivi/switch/colore bolle: route production autenticati mostrano ancora `ui.state.domain = general`, `stateSnapshot.activeDomains = []`, `leadDomain = general` e `assistant.domain = general` sia per prompt monodominio training (`cmn9g02d30014128naqhn91jr`) sia per follow-up health+training sulla stessa conversazione; PASS solo su assenza del banner alto `modalità specialista attiva` nell'HTML production autenticato; Safari WebDriver bloccato localmente da `Allow remote automation` ma il problema app e` comunque confermato dai payload production reali | prossimo=aprire fix backend stretto sul path che produce stato dominio `general` invece di domini canonici per la UI
2026-03-27 23:08 | ruolo=backend-developer | fatto=fix backend stretto sul path production-facing che degradava i domini canonici a `general` | output=`src/lib/ai/orchestrator/orchestrator.ts` ora costruisce un `stateSnapshot` canonico arricchito con `leadDomain`, `activeDomains` e `domainPanels` coerenti con routing, speaker e snapshot precedente; `tests/api/chat-orchestration.test.ts` aggiunge i casi `training` e `health+training`; `tests/api/chat-send-persistence.test.ts` blocca `assistant.domain` allineato al `leadDomain`; verdi `60/60` test mirati, `npm run typecheck`, `npm run lint`, `npm run build` | prossimo=publish remoto e nuova smoke production sui due scenari QA già falliti
2026-03-27 23:28 | ruolo=backend-developer | fatto=publish e verifica production del fix backend domini/chat | output=commit applicativo `507bbb9` pushato su `origin/main`; deploy Vercel production completato su `https://livewell-kpaijav1b-iamotts-projects.vercel.app` con alias `https://livewell.mottisi.com`; smoke production PASS sulla conversazione `cmn9gm9iy000111ahsyh3lu1r`: prompt training -> `ui.state.domain=training`, `leadDomain=training`, `activeDomains=['training']`, `assistant.domain=training`; follow-up health+training -> `ui.state.domain=health`, `leadDomain=health`, `activeDomains` contiene `health` e `training`, `assistant.domain=health` | prossimo=nessun fix obbligatorio aperto nel perimetro; attendere solo eventuale QA browser-side di conferma visiva
2026-03-27 23:46 | ruolo=backend-developer | fatto=chiusura locale del fix pack prodotto su refresh feedback, reasoning/live, bootstrap documenti e reset password | output=creato backup locale `bkp/backups/2026-03-27/2346_product-triage-fixes`; `FeedbackWidget` ricarica i feedback persistiti al refresh; `auth.config.ts` rende pubbliche `/forgot-password` e `/reset-password`; `live-sync` emette `thinkingSteps` e il transcript live li persiste; `ChatInput`/`ChatContext` sincronizzano il `conversationId` live nel runtime condiviso; il reasoning non mostra piu` uno specialista predetto prima del messaggio completo; `live-token` include documenti e artefatti recenti nel `systemInstruction`; validate verdi con 18 test mirati, `npm run typecheck`, `npm run lint`, `npm run build` | prossimo=commit, push, deploy e verdict finale onesto sui problemi ancora aperti di prodotto
2026-03-27 23:58 | ruolo=backend-developer | fatto=publish remoto del fix pack prodotto con verifica alias | output=commit applicativo `e8d558d` pushato su `origin/main`; deploy Vercel production completato su `https://livewell-5pmsk3h6e-iamotts-projects.vercel.app` e aliasato su `https://livewell.mottisi.com`; verifica HTTP finale positiva con redirect auth `307` verso `/login` | prossimo=chiudere il turno con verdict `ISSUES FOUND` perche` restano gap reali di prodotto fuori dal perimetro corretto del fix pack
2026-03-28 00:13 | ruolo=backend-developer | fatto=chiusura locale del fix pack su mapping agente→dominio canonico e quick replies contestuali | output=creato backup locale `bkp/backups/2026-03-28/0008_product-reliability-pre`; introdotto `src/lib/ai/team/domainMapping.ts` come single source of truth per il dominio runtime/visuale degli agenti; `AgentProfile`/loader/schema supportano `primaryDomain`; `sleep-coach` dichiara `primaryDomain=mindfulness`; i path production-facing (`chat/send`, `chatStream`, `case/compat`, `case/events`, `case/protocol`, `consensus/domainResolver`, `agentExecution`) non degradano piu` al primo `domainTags[0]`; `contextualQuickReplies` usa l'ultima domanda utile, deduplica le opzioni e sopprime chip fuorvianti sulle domande composte; aggiunti guardrail in `tests/api/agent-domain-mapping.test.ts`, `tests/api/contextual-quick-replies.test.ts` e aggiornato `tests/api/chat-send-persistence.test.ts`; verdi 27 test mirati, 76 test di confine, `npm run typecheck`, `npm run lint`, `npm run build` | prossimo=commit, push, deploy e review finale onesta dei gap di prodotto ancora aperti
2026-03-28 00:20 | ruolo=backend-developer | fatto=publish remoto del fix pack dominio/quick replies con verifica alias | output=commit applicativo `22e25d2` pushato su `origin/main`; deploy Vercel production completato su `https://livewell-lu2mehcyp-iamotts-projects.vercel.app` e aliasato su `https://livewell.mottisi.com`; rerun post-commit verdi con 103 test di confine; verifica HTTP finale positiva con redirect auth `307` verso `/login` | prossimo=chiudere il turno con verdetto finale onesto: miglioramenti reali pubblicati, ma restano gap di prodotto ancora aperti
