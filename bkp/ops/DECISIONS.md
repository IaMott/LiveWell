Decision log (ADR light)

2026-03-26 23:07 | ADR-2026-03-26-2307 | contesto=fix del warning residuo GitHub Actions dopo il rientro della CI rossa | decisione=applicare il bump minimo del workflow a `actions/checkout@v5` e `actions/setup-node@v5`, senza toccare `node-version: '20'` del job e senza alterare altri step CI | motivo=il warning del run corrente riguarda il runtime interno delle GitHub Actions e non la versione Node usata dal progetto; il fix piu sicuro e mirato e` aggiornare solo le action alla prima major che usa Node 24 | impatto=.github/workflows/ci.yml | stato=attiva

2026-03-26 22:55 | ADR-2026-03-26-2255 | contesto=fix dei failure CI piu recenti dopo la chiusura Fase 1 | decisione=trattare come regressione nuova solo il mock Prisma incompleto nel test `conversation-thinking-export`, e come failure preesistente da riallineare il test `orchestrator-interview-flow` rispetto alla semantica attuale di `round2ForPersistence` con peer stub proposals | motivo=i log GitHub Actions mostrano che il failure orchestrator era gia presente prima degli ultimi commit di Fase 1, mentre il crash su `prisma.caseState.findUnique` nasce direttamente dall'introduzione del reload conversazione con `stateSnapshot`; il fix minimo e piu sicuro e` quindi test-only | impatto=tests/api/conversation-thinking-export.test.ts, tests/api/orchestrator-interview-flow.test.ts | stato=attiva

2026-03-26 21:30 | ADR-2026-03-26-2130 | contesto=review finale post-chiusura Fase 1 shared text/live | decisione=classificare come debiti tecnici non bloccanti da portare alla fase successiva: 1) shape core `CaseState` ancora legacy-backed, 2) tool execution live ancora mono-agent per turno, 3) transport/UI ancora ibridi tra `stateSnapshot` e `activeSpecialist*` | motivo=la Fase 1 e` chiusa sul criterio canonical-first minimo, ma questi tre punti restano i principali ostacoli alla rimozione completa del legacy e alla piena semantica panel-native | impatto=src/lib/ai/case/state.ts, src/lib/ai/case/compat.ts, src/lib/ai/case/persistence.ts, src/app/api/chat/live-sync/route.ts, src/app/api/chat/send/route.ts, src/components/chat/ChatShell.tsx, src/contexts/ChatContext.tsx | stato=attiva

2026-03-26 21:17 | ADR-2026-03-26-2117 | contesto=chiusura effettiva della Fase 1 shared text/live dopo attivazione del primary path canonico | decisione=considerare Fase 1 chiusa quando `stateSnapshot` e` sia write-path primario sia read-path primario, con compat layer ridotto a fallback e test finali verdi su persistence/reload/live-sync/live-token | motivo=questo e` il punto minimo che elimina la dipendenza primaria dal legacy senza aprire Fasi 2-13; oltre questo resta solo cleanup differibile del legacy storico | impatto=prisma/schema.prisma, src/lib/ai/case/persistence.ts, src/app/api/chat/send/chatPersistence.ts, src/app/api/live-token/route.ts, tests/api/case-persistence.test.ts, tests/api/conversation-stateSnapshot-route.test.ts, tests/api/live-sync-stateSnapshot.test.ts | stato=attiva

2026-03-26 19:04 | ADR-2026-03-26-1904 | contesto=chiusura del perimetro minimo server-side sul gap runtime `stateSnapshot` | decisione=nel route `chat/send` materializzare sempre `nextCaseState` da `consensus.stateSnapshot` quando `consensus.caseState` manca, e persisterlo comunque come stato legacy | motivo=e il punto minimo corretto per aumentare la disponibilita di `CaseState` che alimenta sia `/api/conversations/[id]` sia `/api/chat/live-sync`, senza toccare client o schema Prisma | impatto=src/app/api/chat/send/route.ts | stato=attiva

2026-03-26 19:02 | ADR-2026-03-26-1902 | contesto=analisi dei gap runtime `stateSnapshot` in production | decisione=trattare i due gap come problema di disponibilita del `CaseState` ricostruibile, non come bug primario del transport client | motivo=entrambi i route (`/api/conversations/[id]` e `/api/chat/live-sync`) costruiscono `stateSnapshot` da `CaseState`; se lo stato non esiste o non viene prodotto/persistito in quel turno, il campo viene omesso correttamente dal JSON | impatto=src/app/api/chat/send/route.ts, src/app/api/chat/send/chatPersistence.ts, src/app/api/chat/live-sync/route.ts, src/app/api/conversations/[id]/route.ts, src/lib/ai/case/persistence.ts | stato=attiva

2026-03-26 18:42 | ADR-2026-03-26-1842 | contesto=smoke autenticato production del perimetro text/live | decisione=classificare come PASS il bootstrap live autenticato, come PARTIAL il reload conversazione rispetto a `stateSnapshot` e come PARTIAL l'uso client della response di `live-sync`, perche nella prova reale la response di `live-sync` non include `stateSnapshot` e il payload reale di `/api/conversations/[id]` osservato non lo mostra | motivo=le evidenze runtime production prevalgono sul solo wiring codice | impatto=src/app/api/conversations/[id]/route.ts, src/app/api/chat/live-sync/route.ts, src/components/chat/ChatInput.tsx | stato=attiva

2026-03-26 18:35 | ADR-2026-03-26-1835 | contesto=review finale del perimetro minimo text/live post-fix | decisione=considerare chiusi i tre finding minimi richiesti (reload server-side, fallback globali cross-conversation, uso client della response di `live-sync`), mantenendo come rischio residuo non bloccante il doppio binario `live-sync response` + refresh server-side in `LiveModal` | motivo=il perimetro verificato e` coerente end-to-end; il refresh server-side resta una safety net e non un bug confermato | impatto=src/app/api/conversations/[id]/route.ts, src/contexts/ChatContext.tsx, src/components/chat/ChatShell.tsx, src/components/chat/live/LiveModal.tsx, src/components/chat/ChatInput.tsx | stato=attiva

2026-03-26 18:32 | ADR-2026-03-26-1832 | contesto=chiusura del rischio residuo tra refresh post-turn live e persistenza asincrona di `live-sync` | decisione=usare in `ChatInput` la response gia esistente di `/api/chat/live-sync` per salvare subito lo snapshot conversation-scoped e notificare i consumer con un `StorageEvent` sintetico nella stessa tab | motivo=e il punto minimo corretto dove la sync live e gia chiamata; riduce la race senza duplicare chiamate in `LiveModal` o toccare il route server | impatto=src/components/chat/ChatInput.tsx | stato=attiva

2026-03-26 18:30 | ADR-2026-03-26-1830 | contesto=chiusura del fallback globale residuo nel consumer live client | decisione=rimuovere in `LiveModal` ogni lettura/scrittura della chiave globale `livewell_case_state_snapshot`, mantenendo solo bootstrap server, chiavi conversation-scoped e snapshot in memoria della sessione | motivo=e il fix minimo corretto per eliminare la contaminazione cross-conversation dal ramo live senza riaprire `ChatInput`, `live-sync` o Prisma | impatto=src/components/chat/live/LiveModal.tsx | stato=attiva

2026-03-26 18:28 | ADR-2026-03-26-1828 | contesto=chiusura del finding residuo sui fallback globali nello shell text/live | decisione=rimuovere in `ChatShell` ogni lettura della chiave globale `livewell_case_state_snapshot`, mantenendo solo lo snapshot conversation-scoped e i fallback legacy gia forniti dal context | motivo=e il fix minimo corretto per chiudere il finding senza toccare `LiveModal`, `ChatInput` o il ramo server | impatto=src/components/chat/ChatShell.tsx | stato=attiva

2026-03-26 18:22 | ADR-2026-03-26-1822 | contesto=review Fase 1 shared text/live dopo i tre blocchi minimi | decisione=classificare come finding reale la permanenza di fallback globali `livewell_case_state_snapshot` in `ChatShell` e `LiveModal`, e come rischio residuo la race tra refresh post-turn live e persistenza asincrona di `live-sync` | motivo=il reload server-side e` corretto, ma l'assenza totale di contaminazione cross-conversation e l'allineamento immediato del post-turn live non sono ancora chiusi end-to-end | impatto=src/components/chat/ChatShell.tsx, src/components/chat/live/LiveModal.tsx, src/app/api/chat/live-sync/route.ts | stato=attiva

2026-03-25 23:38 | ADR-2026-03-25-2338 | Decisione: mantenere `birthDate` come chiave runtime/storage canonica interna ma guidare prompt e intake con `birth_date` dove utile, usando bridge esplicito in `handlers.ts`, `inputInference.ts` e `intakeQuestions.ts`. Motivo: il piano utente richiedeva l'abbandono di `age` a favore della data di nascita senza riaprire in questo step tutti i path legacy che leggono `birthDate`; il fix corretto era allineare prompt/fallback/intake preservando la compatibilita interna. Stato: implementata.

2026-03-25 23:38 | ADR-2026-03-25-2338-B | Decisione: per il live mode passare il `conversationId` esistente gia alla richiesta di `/api/live-token`, cosi `buildLiveSystemInstruction()` puo leggere il `CaseState` corretto e iniettare lo specialista attivo della conversazione corrente. Motivo: senza questo passaggio il live token ricadeva sul caso piu recente dell'utente o su contesto generico, vanificando parte del piano B3. Stato: implementata.

2026-03-23 21:46 | ADR-2026-03-23-2146 | Decisione: classificare il caso dello screenshot come bug reale di protocollo e non come semplice hallucination del prompt. Motivo: il runtime riconosce correttamente richieste esplicite di specialista (`passami il`, `nutrizionista`) in `routing.ts`, e in `owner_active` sa aprire `consult_active_takeover`; pero nel ramo `consult_active_takeover` di `protocol.ts` una richiesta verso un terzo specialista (`requestedAgentId != consultTarget`) finisce nel branch di `return_baton` al proprietario invece di aprire il nuovo consulto. Il thinking del fisiatra aggrava il sintomo, ma la causa primaria e la state machine. Stato: attiva.

2026-03-23 19:44 | ADR-2026-03-23-1944 | Decisione: per la richiesta su ultimo commit/autore/data usare `git log -1` file-by-file e riportare `NON TRACCIATO` per `src/lib/ai/orchestrator/multiDomainTriage.ts`. Motivo: l'utente chiede lo storico Git degli stessi 13 file della staging list; 12 file hanno storico repository, mentre `multiDomainTriage.ts` e un file nuovo del working tree e non deve essere forzato dentro una risposta Git inventata. Stato: attiva.

2026-03-23 19:40 | ADR-2026-03-23-1940 | Decisione: per la richiesta utente sui tempi di modifica usare come fonte i timestamp locali del filesystem (`stat`) sui 13 file approvati, non la cronologia Git. Motivo: la domanda chiede la data e ora di ultima modifica dei file nel working tree attuale; l'informazione corretta e piu diretta e quindi il `mtime` locale, specialmente con file non ancora committati come `multiDomainTriage.ts`. Stato: attiva.

2026-03-23 19:23 | ADR-2026-03-23-1923 | Decisione: fissare come staging list finale del futuro commit il set di 13 file applicativi del feature diff multi-dominio/quick replies, lasciando fuori tutti i file memoria `bkp/ops/**` anche se modificati durante l'analisi. Motivo: il task utente richiede esplicitamente di preparare la staging list del codice da committare senza eseguire ancora il commit; includere la memoria operativa altererebbe il perimetro del rilascio applicativo e mescolerebbe tracking interno e feature product. Stato: attiva.

2026-03-23 19:21 | ADR-2026-03-23-1921 | Decisione: trattare come blocco coerente da publish solo il feature diff quick-replies/triage multi-dominio composto dai file tracked del flusso chat/orchestrator/domain detection piu `src/lib/ai/orchestrator/multiDomainTriage.ts`, ed escludere dal futuro commit `src/lib/ai/orchestrator/routingLegacy.ts`, `tests/orchestrator-routing.test.ts` e `.claude/**`. Motivo: il runtime attuale referenzia il nuovo path `resolveRoutingCandidates()` e il test vivo `tests/api/orchestrator-routing-phase1.test.ts` dichiara esplicitamente il vecchio test/legacy routing come morto e sostituito; inoltre `routeMessage` non esiste piu nel codice attuale e `.claude/worktrees/*` sono artefatti operativi esterni al diff applicativo. Stato: attiva.

2026-03-23 19:19 | ADR-2026-03-23-1919 | Decisione: classificare il working tree locale corrente come diff applicativo non pubblicato, distinto da `HEAD`/`origin/main`, e sospendere esplicitamente commit/push/deploy fino a conferma utente. Motivo: l'analisi repository-backed mostra `main` allineato a `origin/main` sul commit `ba7170a`, mentre il working tree contiene un blocco coerente di feature multi-dominio (quick replies + triage + raffinamento detection) piu alcuni residui untracked `.claude/`; il task richiesto era solo ricostruire cosa manca alla pubblicazione senza eseguirla. Stato: attiva.

2026-03-19 21:22 | ADR-0021 | review mirata del commit `442523a`
- I fix del commit `442523a` meritano conferma solo parziale.
- I test dichiarati dimostrano bene consulti impliciti e handoff same-domain, ma non coprono in modo sufficiente tutto il cluster del monodominio implicito.
- Il prossimo passo corretto, se richiesto, e un micro-fix mirato su owner impliciti residui e takeover same-domain troppo aggressivo; non una campagna sistemica piu ampia.

2026-03-19 20:54 | ADR-0020 | micro-fix residui impliciti post-validazione avanzata
- `chooseInitialOwner()` resta il punto giusto per la selezione implicita del primo specialista quando il dominio e gia forte.
- `registry.ts` resta il punto giusto per ranking consult/handoff impliciti specialistici.
- `protocol.ts` gestisce solo la continuita conversazionale same-domain, senza introdurre nuovi stati o refactor del `CaseState`.
- Sostituisce l'idea di toccare `agentSelection.ts` o introdurre un ranking engine piu grande.

2026-03-18 21:08 | ADR-2026-03-18-2108 | Decisione: usare `llm-architect` come ruolo implicito per questo task. Motivo: richiesta centrata su architettura LLM multi-agent, governance runtime, state model, artifact generation e refactoring strutturale. Stato: attiva.

2026-03-18 21:08 | ADR-2026-03-18-2108-B | Decisione: trattare `capabilities.md` come documentazione non eseguita dal runtime fino a prova contraria. Motivo: `src/lib/ai/team/loader.ts` carica solo `profile.json` + `prompt.md`; nessun path runtime risolve o valida `capabilities.md`. Stato: attiva.

2026-03-18 22:35 | ADR-2026-03-18-2235 | Decisione: il primo refactor incrementale deve introdurre subito un `CaseState` canonico con owner del caso, active speaker, consulto attivo e return target. Motivo: e il minimo slice che sposta la collaborazione da teatralizzazione SSE/prompt a protocollo persistito e tracciabile. Stato: attiva.

2026-03-18 22:35 | ADR-2026-03-18-2235-B | Decisione: `activeSpecialist` va mantenuto solo come campo di compatibilita derivato in uscita, non piu come fonte di verita. Motivo: evita doppia ownership e consente migrazione graduale di `route.ts`, `chatStream.ts` e `ChatContext.tsx` senza toccare la UI. Stato: attiva.

2026-03-18 23:15 | ADR-2026-03-18-2315 | Decisione: persistere `CaseState` in una nuova tabella `case_states` dedicata, non in `AgentWorkspace`. Motivo: `AgentWorkspace` resta memoria locale dell'agente; `CaseState` deve essere stato canonico del protocollo. Stato: implementata.

2026-03-18 23:15 | ADR-2026-03-18-2315-B | Decisione: trattare `activeSpecialist` come compatibilita derivata solo in due casi: takeover reale o richiesta esplicita iniziale dell'utente verso uno specialista. Motivo: evitare che un owner auto-inizializzato blocchi impropriamente il routing o la raccolta dati. Stato: implementata.

2026-03-18 23:30 | ADR-2026-03-18-2330 | Decisione: introdurre `handoff_pending_user` come checkpoint umano backend-native prima dell'handoff permanente. Motivo: serviva un passaggio verificabile dal runtime che separasse takeover temporaneo da cambio owner definitivo senza richiedere modifiche UI. Stato: implementata.

2026-03-18 23:30 | ADR-2026-03-18-2330-B | Decisione: caricare `TEAM/**/capabilities.md` nel runtime come capability contracts minimali, usando parsing markdown e fallback conservativi. Motivo: spostare competenze, limiti, trigger e artefatti fuori da documentazione morta e dai prompt sparsi. Stato: implementata.

2026-03-18 23:30 | ADR-2026-03-18-2330-C | Decisione: governare `artifactsToSave` tramite `src/lib/ai/artifacts/governance.ts` e contratti runtime dello specialista, sostituendo il merge diretto in `consensusEngine.ts`. Motivo: gli artefatti dovevano diventare responsabilita architetturale e non side effect libero dei recommendation text. Stato: implementata.

2026-03-18 23:48 | ADR-2026-03-18-2348 | Decisione: rendere illegale `activeSpecialistId` come input backend del runtime e lasciarlo solo come compatibilita derivata in output. Motivo: chiudere la seconda verita residua sul caso senza toccare il client. Stato: implementata.

2026-03-18 23:48 | ADR-2026-03-18-2348-B | Decisione: confinare `resolveRoutingContext` in `src/lib/ai/orchestrator/routingLegacy.ts` e lasciare `routing.ts` focalizzato sul path nuovo candidate-only. Motivo: isolare il legacy ancora necessario senza farlo sembrare parte del percorso vivo del nuovo modello. Stato: implementata.

2026-03-18 23:48 | ADR-2026-03-18-2348-C | Decisione: estrarre i builder di contesto da `synthesis.ts` in `synthesisContext.ts` e irrobustire il parser di `capabilities.md` con alias heading e normalizzazione markdown. Motivo: ridurre il monolite residuo e rendere i capability contracts piu affidabili a runtime. Stato: implementata.

2026-03-19 00:14 | ADR-2026-03-19-0014 | Decisione: considerare `typecheck`, `build` e suite test mirata del refactor multi-agent come publish gate sufficiente per questo rilascio, nonostante `npm run lint` resti rumoroso su output generati esterni al diff sotto `.claude/worktrees/.../.next/`. Motivo: il blocco lint non deriva dal refactor pubblicato e non deve impedire commit/push/deploy del backend gia validato. Stato: attiva.

2026-03-19 00:35 | ADR-2026-03-19-0035 | Decisione: trattare come debt residuo prioritario solo i punti che restano semanticamente vivi nel runtime nuovo (`activeSpecialist` come hint secondario, re-export legacy da `routing.ts`, visibilita specialistica derivata da proposal in `synthesis`). Motivo: la review finale conferma che il protocollo canonico non e in discussione; il cleanup ulteriore deve limitarsi a ridurre compatibilita e semantiche residue, senza riaprire il refactor. Stato: attiva.

2026-03-19 00:38 | ADR-2026-03-19-0038 | Decisione: completare il cleanup residuo lasciando `activeSpecialist` solo come output compatibile e rimuovendo ogni suo uso pre-orchestrazione o semantico non necessario. Motivo: il campo puo restare per compatibilita UI, ma non deve piu pilotare behavior secondari fuori dal payload finale. Stato: implementata.

2026-03-19 00:42 | ADR-2026-03-19-0042 | Decisione: pubblicare il cleanup backend con lo stesso gate ristretto del diff (`typecheck` + test mirati) senza riaprire build/lint generale. Motivo: il task richiedeva solo publish del cleanup gia verificato, e il perimetro modificato era confinato a sei file backend/test senza impatto UI. Stato: attiva.

2026-03-19 00:48 | ADR-2026-03-19-0048 | Decisione: considerare il protocollo di caso runtime come reale ma la validazione complessiva del sistema solo parziale, finche consult/handoff triggers dei capability contracts non guideranno davvero il protocollo e l'artifact governance non validara prerequisiti minimi oltre al solo storage type. Motivo: la campagna di validazione ha confermato il cuore del protocollo, ma ha trovato gap reali su contratti e artifact che impediscono di dichiarare il sistema completamente affidabile. Stato: attiva.
2026-03-19 01:01 | ADR-2026-03-19-0101 | Decisione: correggere B1-B5 con interventi minimi sui layer gia esistenti, usando i capability contracts come gate runtime nel protocollo e `medicalRecord.missingKeys` come prerequisito minimo equivalente per artifact/output, senza riaprire il refactor generale. Motivo: i bug emersi riguardavano enforcement e priorita semantica, non la struttura base del sistema; il fix corretto era stringere i guardrail nei moduli gia canonici. Stato: implementata.
2026-03-19 01:14 | ADR-2026-03-19-0114 | Decisione: classificare B4 come solo parzialmente corretto in review finale, pur con test verdi, finche `src/lib/ai/artifacts/contracts.ts` mantiene una istruzione residua che consente assunzioni ragionevoli con dati mancanti. Motivo: il branch di gating in `synthesis.ts` e corretto, ma la coerenza interna del sistema resta incompleta e non va sovrastimata. Stato: attiva.
2026-03-19 01:17 | ADR-2026-03-19-0117 | Decisione: chiudere B4 con un micro-fix esclusivo in `src/lib/ai/artifacts/contracts.ts`, senza toccare `synthesis.ts` o altri moduli, e rafforzare solo il test di synthesis gia esistente. Motivo: il residuo era testuale/semantico e localizzato; la correzione minima corretta era allineare l'istruzione residua al gating prudente gia implementato. Stato: implementata.
2026-03-19 10:03 | ADR-2026-03-19-1003 | Decisione: non dichiarare il sistema corretto o pienamente affidabile dopo la campagna estesa di validazione, finche non verranno corretti almeno il triage nutrizionale implicito, il ranking dei consult target impliciti e i checkpoint di handoff su cambio dominio non esplicito. Motivo: i saluti/generici, l'artifact governance e i gate principali reggono, ma 25 FAIL e 16 PARTIAL mostrano che i casi reali multi-turno e i passaggi impliciti restano troppo fragili. Stato: attiva.
2026-03-19 10:40 | ADR-2026-03-19-1040 | Decisione: trattare il sistema come runtime multi-agent reale ma ancora solo parzialmente credibile a livello sistemico, finche non verranno corretti almeno triage implicito cross-domain, ranking del consult target, takeover naturale e handoff impliciti. Motivo: la campagna da 230 scenari conferma il protocollo, la governance artifact e la memoria backend, ma mostra che la collaborazione implicita resta troppo euristica per dichiarare l'intera piattaforma corretta o affidabile in modo pieno. Stato: attiva.
2026-03-19 12:13 | ADR-2026-03-19-1213 | Decisione: correggere i residui cross-domain emersi dalla campagna sistemica con micro-fix locali su detection, ranking consult target, continuity intent, gating strutturato e supporto artifact dei ruoli ibridi, senza riaprire il refactor generale. Motivo: i problemi residui erano euristiche troppo strette o troppo semplici nei layer gia canonici; la correzione minima corretta era aumentare il realismo semantico del runtime mantenendo intatta l'architettura. Stato: implementata.
2026-03-19 14:37 | ADR-2026-03-19-1437 | Decisione: considerare il commit `41612ca` come miglioramento reale ma non ancora sufficiente per dichiarare il sistema corretto o pienamente affidabile, finche non verranno chiusi i residui su handoff impliciti, casi sporchi multi-tema e queue persistence dell'interview flow. Motivo: la campagna comparativa da 325 scenari mostra un netto miglioramento rispetto al baseline da 230 scenari, ma conferma ancora 46 FAIL, 36 PARTIAL e 2 regressioni repository reali sulla persistenza della coda domande. Stato: attiva.
2026-03-19 18:15 | ADR-2026-03-19-1815 | Decisione: chiudere i residui post-validazione comparativa con micro-fix stretti sui layer canonici gia esistenti (`interviewFlow`, `protocol`, `registry`, `synthesis`) e una suite di test mirati, senza introdurre nuove astrazioni o riaprire il refactor. Motivo: i bug residui erano localizzati su ricostruzione queue, continuity intent, ranking semantico di pochi consulti impliciti e plan gating strutturato; il fix corretto era ripristinare il comportamento credibile con modifiche conservative. Stato: implementata.
2026-03-19 19:40 | ADR-2026-03-19-1940 | Decisione: considerare i fix del commit `44df3a1` come miglioramento reale e verificato, ma non ancora finale, finche non verranno eventualmente chiusi i residui residui su monodominio implicito, consulti impliciti di alcuni specialisti health/legal e handoff same-domain. Motivo: la campagna avanzata da 369 scenari e 66 test repository verdi conferma la chiusura della regressione queue e il miglioramento di gating/handoff/consulti, ma lascia ancora 42 FAIL e 13 PARTIAL concentrati soprattutto su routing implicito e passaggi collaborativi non perfettamente uniformi. Stato: attiva.
2026-03-19 22:13 | ADR-2026-03-19-2213 | Decisione: nei casi same-domain il phrasing di semplice continuita (`restiamo su questa parte`, `proseguiamo con lui`) deve mantenere `consult_active_takeover`, mentre `handoff_pending_user` deve richiedere segnali piu stabili di ownership (`mi seguisse`, `questo percorso`, recovery/therapy path espliciti). Motivo: la review finale ha confermato che il protocollo era troppo aggressivo nel promuovere a handoff frasi che esprimono prosecuzione momentanea, non trasferimento stabile del caso. Stato: implementata.
2026-03-19 22:13 | ADR-2026-03-19-2213-B | Decisione: il consulto implicito `consulente-legale` deve emergere solo con segnali family-law o giuridici forti, non dalla sola parola `separazione`/`separando`. Motivo: il residuo reale osservato era un falso positivo legale su messaggi emotivi di separazione; il fix corretto era stringere trigger e ranking senza indebolire i casi con `affido`, `accordi`, `tutela`, `legale`. Stato: implementata.
2026-03-20 11:39 | ADR-2026-03-20-1139 | Decisione: classificare il commit `c9b21d2` come conferma parziale, non piena. Motivo: la review repository-backed conferma il miglioramento su owner implicito e l'assenza di regressioni, ma trova ancora due residui reali: il consulto legal non si apre su `ci sono problemi legali con la separazione` e `parliamo ancora di questo con lui` promuove ancora troppo presto a `handoff_pending_user`. Stato: attiva.
2026-03-20 11:59 | ADR-2026-03-20-1159 | Decisione: trattare `legal*` come segnale giuridico valido anche nelle forme flesse (`legali`) quando compare insieme a `separazione`, ma mantenere nullo il consulto legal sui messaggi solo emotivi di separazione. Motivo: il residuo reale non era mancanza di logica family-law, ma una regex troppo stretta che non agganciava `problemi legali`; il fix corretto era allargare il lemma giuridico senza rendere sufficiente la sola separazione. Stato: implementata.
2026-03-20 11:59 | ADR-2026-03-20-1159-B | Decisione: `parliamo ancora di questo con lui` va trattato come continuità di takeover e non come trigger di handoff same-domain. Motivo: la review finale ha mostrato che quel phrasing esprime prosecuzione conversazionale, non ancora trasferimento stabile del caso; il fix corretto era rimuoverlo dai pattern di handoff, lasciandolo solo nei pattern di takeover. Stato: implementata.
2026-03-20 12:35 | ADR-2026-03-20-1235 | Decisione: classificare il commit `f8093cb` come conferma parziale, non piena. Motivo: il fix legal e il fix takeover richiesto sono reali e senza regressioni, ma il runtime non promuove ancora a handoff un phrasing forte come `vorrei che fosse lui a seguirmi da ora`, quindi la semantica same-domain non è ancora perfettamente allineata ai criteri richiesti. Stato: attiva.
2026-03-20 12:42 | ADR-2026-03-20-1242 | Decisione: nel same-domain handoff i phrasing che esprimono preferenza stabile di presa in carico (`vorrei che fosse lui/lei a seguirmi da ora`, `vorrei continuare con lui/lei come riferimento principale`) devono aprire `handoff_pending_user`, mentre i phrasing di semplice continuità (`parliamo ancora di questo con lui`, `restiamo su questa parte`, `proseguiamo con lui`) restano `consult_active_takeover`. Motivo: il residuo confermato dalla review riguardava solo la mancata promozione dei segnali più forti di ownership stabile; il fix corretto era aggiungere pattern stretti e semantici senza riaprire la state machine. Stato: implementata.
2026-03-20 12:51 | ADR-2026-03-20-1251 | Decisione: classificare il commit `1a2101d` come conferma piena del cluster same-domain handoff/takeover. Motivo: inspection, test repository e harness con team reale confermano che i phrasing forti aprono `handoff_pending_user`, i phrasing morbidi restano takeover, i return all'owner restano corretti e non emergono regressioni sulle aree di guardia. Stato: attiva.
2026-03-20 15:50 | ADR-2026-03-20-1550 | Decisione: classificare il sistema sul baseline `1a2101d` come multi-agente runtime-driven reale ma ancora solo parzialmente affidabile in uso controllato. Motivo: la campagna sistemica finale da 525 scenari conferma come aree ormai credibili protocollo, consulti espliciti, takeover/handoff, memory/context, gating, artifacts, SSE/upload e path espliciti; restano però 45 FAIL e 18 PARTIAL concentrati soprattutto nel monodominio implicito cross-domain, in alcuni consulti impliciti specifici e nei casi sporchi/ambigui ad alta entropia. Stato: attiva.
2026-03-20 16:07 | ADR-2026-03-20-1607 | Decisione: correggere i residui sistemici ancora aperti con micro-fix stretti nei layer canonici `domainDetection.ts`, `protocol.ts` e `registry.ts`, senza toccare `CaseState`, UI o orchestrazione generale. Motivo: la campagna finale ha mostrato che il debito residuo è semantico su implicito cross-domain, consult target ranking e dirty cases; il fix corretto è aumentare la copertura semantica e ridurre fallback deboli, non riaprire il refactor. Stato: implementata.
2026-03-20 16:35 | ADR-2026-03-20-1635 | Decisione: classificare il commit `67db453` come conferma parziale, non piena. Motivo: la review repository-backed conferma miglioramenti reali su monodominio implicito e consulti impliciti residui, senza regressioni su persistence/gating/artifacts, ma il team reale mostra ancora un owner debole su `mi sto separando e ci sono problemi pratici` e dirty cases con reason/target solo parzialmente credibili. Stato: attiva.
2026-03-20 17:50 | ADR-2026-03-20-1750 | Decisione: classificare il fail CI su `tests/api/interview-flow-transcript-2026-03-11.test.ts` come test vecchio, non regressione runtime. Motivo: `src/lib/ai/orchestrator/interviewFlow.ts` implementa esplicitamente il batching fino a 3 domande baseline L1 e il test continuava a imporre `<= 1`; il comportamento corretto da preservare è la qualità mirata delle domande e l'assenza di follow-up generici, non il limite storico a una sola domanda. Stato: implementata.
2026-03-20 22:01 | ADR-2026-03-20-2201 | Decisione: classificare il cluster “continuità del focus e qualità conversazionale reale” come debito ancora aperto e materialmente diverso dalla sola correttezza di protocollo. Motivo: la validazione su 30 scenari multi-turno con team reale mostra che owner/speaker/state machine spesso restano tecnicamente coerenti, ma il runtime ricade comunque in intake baseline, perde il problema attivo o devia verso consulti non credibili; i fix futuri devono stringere `interviewFlow`, `domainDetection`, `registry`, `protocol` e `synthesis`, non riaprire l’architettura. Stato: attiva.
2026-03-21 09:16 | ADR-2026-03-21-0916 | Decisione: nei follow-up specialistici già avviati il runtime deve privilegiare il problema attivo rispetto ai campi mancanti strutturali, usando un solo follow-up focalizzato e vietando baseline L1/L2 o domain-plan generici quando esistono segnali specifici, continuità o memoria di caso. Motivo: la review conversazionale ha confermato che il bug principale non era il protocollo ma la priorità delle domande; il fix corretto è stringere `interviewFlow` e rinforzare `domainDetection`/`protocol`/`registry`/`synthesis` senza riaprire il refactor. Stato: implementata.
2026-03-21 09:45 | ADR-2026-03-21-0945 | Decisione: chiudere i due residui conversazionali rimasti con micro-fix stretti solo su `domainDetection.ts`, `protocol.ts` e `registry.ts`, spostando il caso `separazione + figli + soldi + problemi pratici` verso `coordination/life-organizer` e bloccando consulti extra sui resume sleep puri già in carico a `sleep-coach`. Motivo: la review finale non mostrava un problema architetturale ma due bias semantici specifici; il fix corretto è rafforzare detection e ranking senza riaprire `interviewFlow`, `synthesis` o il protocollo generale. Stato: implementata.
2026-03-23 21:58 | ADR-2026-03-23-2158 | Decisione: correggere il bug feedback/export in due punti, non solo nell'export. Motivo: il difetto nasceva da una doppia causa, cioe `messageId` dell'assistant incoerente tra placeholder client, SSE e persistenza DB, piu export troppo rigido sul match esatto; il fix corretto e allineare l'id a monte e mantenere un fallback cronologico per recuperare i feedback legacy gia salvati con id transitorio. Stato: implementata.
2026-03-23 22:17 | ADR-2026-03-23-2217 | Decisione: persistire i ragionamenti agenti senza migrazione schema, serializzandoli in un marker nascosto nel `content` del messaggio assistant e centralizzando encode/decode in `src/lib/chat/thinkingPersistence.ts`. Motivo: il task richiedeva un fix stretto e pubblicabile subito; questa soluzione conserva i thinking traces nel DB e li rende scaricabili/ripristinabili in chat senza introdurre nuove colonne o dipendere da migrazioni production. Stato: implementata.
2026-03-23 22:35 | ADR-2026-03-23-2235 | Decisione: usare come fonte primaria del reasoning esportabile le proposal reali degli specialisti (`round1/round2Proposals.reasoning`) e non i soli eventi `agent.thinking` di stream. Motivo: gli eventi di stream sono adatti alla UX live ma spesso troppo sintetici (`Analisi in corso`, `Confronto`, `Consenso`); per l'uso di training richiesto dall'utente il dato utile è il reasoning completo già prodotto dall'orchestratore nelle proposal. Stato: implementata.
2026-03-26 00:19 | ADR-2026-03-26-0019 | Decisione: vietare ogni derivazione automatica di `birthDate` a partire da `age` e salvare l'eta come dato separato finche l'utente non fornisce una data di nascita completa reale. Motivo: mese e giorno non possono essere ipotizzati; il fix corretto e rimuovere l'approssimazione dai prompt, dai fallback di inferenza, dai sync legacy e dalla UI, chiedendo la data completa solo quando necessaria. Stato: implementata.
2026-03-26 12:55 | ADR-2026-03-26-1255 | Decisione: trattare il dynamic DB come composizione canonica di `UserAttribute` (semantica+notes+time), `FileAsset` (contenuto utente consultabile) e `RecommendationArtifact` (output AI), senza introdurre una nuova tabella unificante. Motivo: il progetto possiede gia storage storico e accesso multi-agente; il gap reale era semantico. Il fix corretto e stato aggiungere una classificazione osservato/derivabile, usare `UserAttribute` come ledger semantico anche per documenti/artifact e arricchire le viste/API esistenti, evitando una migrazione piu invasiva in produzione. Stato: implementata.
2026-03-26 14:44 | ADR-2026-03-26-1444 | Decisione: classificare il runtime corrente come architettura `single active specialist with collaborative rounds`, non come panel multi-dominio persistentemente attivo. Motivo: il modello canonico di stato usa `ownerAgentId` e `activeSpeakerAgentId` singolari, `resolveRoutingCandidates()` seleziona un set globale di agenti per turno, `executeAgentRounds()` li fa collaborare in due round ma la sintesi finale e la UI promuovono al massimo un solo `activeSpecialist`; i casi multi-dominio senza specialista attivo ricadono inoltre in triage con quick replies. Stato: attiva.
2026-03-26 15:32 | ADR-2026-03-26-1532 | Decisione: preservare scoring, criteri premianti e struttura piramidale, ma riposizionarli come layer subordinato al contesto all'interno di un nuovo modello `multi-domain active panel`. Motivo: l'utente non vuole un routing a trigger/parole chiave come motore principale; il sistema desiderato richiede interpretazione contestuale del caso, preselezione domini, selezione di un agente per dominio e collaborazione continua ad ogni turno, mantenendo pero ranking/gating/fallback come stabilizzatori. Stato: attiva.
2026-03-26 15:38 | ADR-2026-03-26-1538 | Decisione: trattare chat testuale e modalita live Gemini come due interfacce dello stesso runtime canonico, senza introdurre un modello specialistico separato per il live. Motivo: il comportamento richiesto dall'utente impone continuita completa di stato, panel multi-dominio, Dynamic DB e regole agentiche tra testo e audio/video; il ramo live attuale e solo parzialmente allineato e va rifattorizzato sullo stesso contratto shared-by-design, non mantenuto come eccezione. Stato: attiva.
2026-03-26 15:47 | ADR-2026-03-26-1547 | Decisione: eseguire la Fase 1 come refactor di contratti e compatibilita, non di logica di routing/consenso. Motivo: il rischio principale nelle prime modifiche e rompere text/live/reload introducendo un nuovo stato senza un layer di compatibilita; il primo passo corretto e stabilire il contratto canonico shared, mantenerlo dual-write/dual-read e validarlo con una campagna test minima prima di qualunque modifica alle fasi 2-13. Stato: attiva.
2026-03-26 15:58 | ADR-2026-03-26-1558 | Decisione: in `STEP-1` introdurre solo tipi canonici e campi opzionali additive in `src/lib/ai/types.ts`, senza alterare ancora `CaseState`, transport o bootstrap live. Motivo: il modo piu sicuro per avviare la Fase 1 e fissare prima il vocabolario contrattuale shared text/live, evitando regressioni premature su serializzazione, SSE e Gemini Live; i nuovi campi restano dichiarativi finche `STEP-2` e `STEP-3` non completano normalizzazione e compat mapping. Stato: attiva.
2026-03-26 16:02 | ADR-2026-03-26-1602 | Decisione: in `STEP-2` permettere a `normalizeCaseState()` di accettare sia payload legacy sia payload canonical-first, sintetizzando fallback legacy minimi direttamente nello state model. Motivo: `compat.ts` non e ancora stato toccato, ma la Fase 1 richiede che lo stato canonico possa gia essere rappresentato e validato senza rompere il runtime; il compromesso corretto in questa fase e normalizzare presto e mappare in modo piu raffinato nel prossimo step. Stato: attiva.
2026-03-26 16:05 | ADR-2026-03-26-1605 | Decisione: centralizzare in `compat.ts` la traduzione tra `CaseState` legacy e `CanonicalCaseStateSnapshot`, mantenendo in `state.ts` solo la normalizzazione permissiva del payload. Motivo: il compat layer deve diventare il punto unico di convergenza per text e live prima di toccare persistence o transport; questo riduce il rischio che i fallback legacy sintetici restino duplicati e divergenti in piu moduli. Stato: attiva.
2026-03-26 16:08 | ADR-2026-03-26-1608 | Decisione: in `STEP-4` introdurre in `persistence.ts` helper espliciti di dual-read e future dual-write senza modificare ancora i caller Prisma esistenti. Motivo: lo schema `CaseState` del database non ha ancora colonne dedicate al nuovo snapshot canonico; il modo piu sicuro per avanzare nella Fase 1 restando nel perimetro richiesto e preparare la serializzazione condivisa nel modulo di persistenza, mantenendo `toStoredCaseState()` legacy-safe fino all'aggiornamento dei consumer nel passo successivo. Stato: attiva.
2026-03-26 16:16 | ADR-2026-03-26-1616 | Decisione: adottare per primo il nuovo layer di persistenza in `src/app/api/chat/send/chatPersistence.ts`, prima di toccare `chat/send/route.ts`, `live-sync`, `ChatContext` o `live-token`. Motivo: `chatPersistence.ts` e il punto comune che oggi esegue `getCaseState()` e `persistCaseState()` per entrambi i canali; aggiornare prima questo modulo riduce il rischio di divergenza tra text e live e lascia invariati i contratti SSE/UI finché il dual-read/dual-write non e stabile. Stato: attiva.
2026-03-26 16:19 | ADR-2026-03-26-1619 | Decisione: nel primo consumer (`chatPersistence.ts`) attivare subito il dual-read e portare il dual-write fino al bordo del caller, ma senza scrivere il nuovo snapshot nel DB finché lo schema Prisma non offre un campo dedicato o un envelope compatibile. Motivo: questo consente di usare il nuovo contratto nel punto server comune senza introdurre errori di scrittura o campi non riconosciuti dal delegate Prisma. Stato: attiva.
2026-03-26 16:22 | ADR-2026-03-26-1622 | Decisione: propagare per primo `stateSnapshot` nel payload SSE `ui.state` del ramo text, lasciando invariato `chatStream.ts` e serializzando il payload esteso direttamente in `route.ts`. Motivo: il perimetro autorizzato include solo `src/app/api/chat/send/route.ts`; il fix corretto e additivo consiste nel far viaggiare il nuovo snapshot senza rimuovere i campi legacy e senza toccare ancora il tipo condiviso di `chatStream.ts`. Stato: attiva.
2026-03-26 16:39 | ADR-2026-03-26-1639 | Decisione: nel ramo `live-sync` usare il nuovo snapshot canonico come input e fallback operativo, ma senza modificare ancora `live-token` o `LiveModal`. Motivo: dopo l'allineamento del transport text, il passo corretto per mantenere un solo runtime canonico e mettere `live-sync` sullo stesso contratto server-side; il bootstrap live puo restare invariato finché il client non sa ricostruire il nuovo stato. Stato: attiva.
2026-03-26 16:48 | ADR-2026-03-26-1648 | Decisione: nel client adottare per prima la sola lettura e conservazione di `stateSnapshot`, senza ancora usarlo per cambiare la UI. Motivo: il passaggio piu sicuro dopo l'allineamento server-side e rendere `ChatContext` capace di ricevere e mantenere il nuovo stato condiviso senza introdurre regressioni visive premature in `ChatShell` o nel bootstrap live. Stato: attiva.
2026-03-26 16:52 | ADR-2026-03-26-1652 | Decisione: nel primo consumer visuale usare `stateSnapshot` solo come fallback di resa, leggendo il dato gia persistito dal client senza cambiare ancora `useChat` o i contratti del context. Motivo: il perimetro autorizzato e solo `ChatShell.tsx`; il fix piu sicuro e sfruttare il nuovo snapshot gia presente in `localStorage` per migliorare la continuita visiva senza propagare altri cambiamenti strutturali ai consumer adiacenti. Stato: attiva.
2026-03-26 17:02 | ADR-2026-03-26-1702 | Decisione: nel bootstrap live derivare `stateSnapshot` server-side dal `CaseState` legacy e usarlo in modo additivo sia per scegliere il lead specialist sia per arricchire il `systemInstruction`, senza cambiare ancora il contratto richiesto da `LiveModal`. Motivo: il perimetro autorizzato e solo `live-token/route.ts`; il modo piu sicuro per allineare il live al runtime shared e far emergere lo stato canonico gia disponibile nel backend, restituendolo nel payload e usandolo come fallback prioritario senza imporre ancora un consumer client nuovo. Stato: attiva.
2026-03-26 17:21 | ADR-2026-03-26-1721 | Decisione: nel consumer live client leggere `stateSnapshot` dal bootstrap di `live-token`, conservarlo subito come stato shared e usarlo solo in modo additivo per rinforzare il contesto live senza cambiare il contratto di `ChatInput`. Motivo: il perimetro autorizzato e solo `LiveModal.tsx`; il fix piu sicuro e consumare il nuovo snapshot gia disponibile dal backend, mantenendo il comportamento legacy come fallback e senza introdurre una nuova architettura live parallela. Stato: attiva.
2026-03-26 17:40 | ADR-2026-03-26-1740 | Decisione: per chiudere il primo finding reale usare il route di reload conversazione come punto minimo per restituire `stateSnapshot` server-side, ricostruendolo dal `CaseState` legacy corrente senza toccare Prisma o il transport live. Motivo: il collo di bottiglia emerso dalla review e che `ChatContext.loadConversation()` era gia pronto a leggere `stateSnapshot`, ma l'API `/api/conversations/[id]` non lo restituiva; il fix minimo corretto e quindi arricchire solo questo route con gli helper canonici gia introdotti. Stato: attiva.
2026-03-26 17:42 | ADR-2026-03-26-1742 | Decisione: per chiudere il secondo finding reale spostare `stateSnapshot`, `activeSpecialistId` e `activeSpecialistName` da chiavi `localStorage` globali a chiavi scoped per `conversationId`, senza toccare altri consumer. Motivo: il rischio residuo piu concreto era la contaminazione cross-conversation; il perimetro minimo corretto e `ChatContext.tsx`, che centralizza sia il `loadConversation()` sia la gestione degli eventi SSE e dei fallback client. Stato: attiva.
2026-03-26 17:46 | ADR-2026-03-26-1746 | Decisione: nel terzo blocco minimo aggiornare subito il client live dopo il turno assistant tramite refresh server-side della conversazione corrente, invece di tentare di consumare direttamente la response di `live-sync` da `LiveModal`. Motivo: con il perimetro autorizzato limitato a `LiveModal.tsx`, la response di `live-sync` non è accessibile in modo sicuro perché la fetch resta incapsulata in `ChatInput`; la soluzione minima corretta e non distruttiva è usare il route di reload già arricchito con `stateSnapshot` per ridurre l'attesa del solo reload finale senza introdurre doppi sync o doppie tool calls. Stato: attiva.
2026-03-26 19:24 | ADR-2026-03-26-1924 | Decisione: considerare chiuso, nel runtime locale post-fix, il gap minimo sui payload `stateSnapshot` di `/api/conversations/[id]` e `/api/chat/live-sync`. Motivo: smoke autenticato reale sulla conversazione `smokeconv_1774548894` ha mostrato `stateSnapshot` presente in entrambi gli endpoint; resta solo un limite prestazionale del primo `chat/send` nello smoke completo locale, non un'assenza del payload target. Stato: attiva.
2026-03-26 20:15 | ADR-2026-03-26-2015 | Decisione: per il primo fix minimo prestazionale intervenire solo in `ChatContext.tsx` con un recovery server-side post-failure, invece di toccare subito orchestrator o route server. Motivo: il finding reale era un falso timeout client locale sul primo `chat/send` a fronte di un server che completava comunque il turno; il perimetro minimo corretto e` quindi recuperare la conversazione corrente dal server prima di degradare in errore locale, senza cambiare la pipeline AI o i timeout core. Stato: attiva.
2026-03-26 20:20 | ADR-2026-03-26-2020 | Decisione: per il secondo fix minimo prestazionale evitare il round 2 sugli agenti gia` degradati a fallback nel round 1, invece di ritoccare timeout globali o consensus. Motivo: il finding reale era il costo duplicato di un agente lento che scadeva in entrambi i round; il perimetro minimo corretto e` `agentRoundExecution.ts`, dove si puo` riusare la proposal fallback del round 1 mantenendo invariati output shape, `confidence=0` e continuita della pipeline. Stato: attiva.
2026-03-26 20:24 | ADR-2026-03-26-2024 | Decisione: considerare efficaci nel runtime locale i due fix prestazionali minimi appena introdotti. Motivo: nello smoke reale successivo il primo `chat/send` e` rientrato nel timeout client a 30s con `message.complete` presente, e i log non hanno piu mostrato timeout duplicati di `Round 2`; restano solo timeout singoli di `Round 1`, che sono ancora parte del comportamento atteso. Stato: attiva.
2026-03-26 20:53 | ADR-2026-03-26-2053 | Decisione: per chiudere davvero la Fase 1 attivare la persistenza autonoma di `stateSnapshot` con un campo JSON dedicato in `CaseState`, invece di restare su dual-write preparatorio legacy-safe. Motivo: il canonico non poteva diventare primary path finché il DB non aveva un contenitore persistito reale; il perimetro minimo corretto include quindi schema Prisma, migrazione SQL minima, dual-write reale in `chatPersistence` e allineamento del consumer server diretto `live-token` al read path comune. Stato: attiva.
2026-03-26 20:56 | ADR-2026-03-26-2056 | Decisione: considerare il compat layer declassato correttamente nel read path comune quando `fromStoredCaseState()` privilegia `stateSnapshot` e usa il legacy solo come fallback. Motivo: la chiusura architetturale reale della Fase 1 richiede non solo scrivere il canonico, ma anche leggerlo come fonte primaria; il punto canonico minimo era `src/lib/ai/case/persistence.ts`, supportato da un test unitario dedicato al conflitto tra canonico e legacy. Stato: attiva.
